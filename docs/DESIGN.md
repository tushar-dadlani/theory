# Discovery Engine

## Goal

The goal of this discovery engine is to study mathematics and various phenomena by structuring the domain of a problem as rays from infinity and the codomain as rays to infinty through the construction of a hypothetical fano prism with 7 fixed points that define the entire domain and co-domain mapping. 

The 7 points emerge in the following order

1. 1 fixed point at infinity
1. 1 fixed ray coming from infinity 
1. 1st observation of the ray. This defines metric of the unit ray and the first observable fixed point. The hypothetical metric is defined between the two fixed points, this is the first time an observer is introduced in the system.
1. 2nd observation of the ray is the 3rd point. The general case of the 3rd point is that it is non-collinear. This defines a scalene triangle. The non-collinear point defines a closed plane with fixed total angle of 180 degrees. With a triangle we now have 3 observer positions.
1. 1st unfolding operation of the triangle on the triangular plane reveals an infinite line. We can now define the metric triangle ie. an equilateral triangle. This metric triangle can now be tesslated.  


1. The third observation is the ray from the non-collinear 
1. 3rd observation of the ray is from a point that is orthogonal to the ray. This defines the unit equilateral triangle and deifnes the first closed angle system. This triangle is a plane and also is self-similar to the ray to and from infinity due to the sum of the angles being exactly the same. Any triangle is self-similar to the ray from infinity. The triangle in this system is the first unit infinite plane, it did not need the 3rd collinear point to define it we could define it from any 
