# Spectral theory

Spectral algebra and events, the Dirac operator, eigen-systems, self-adjoint structures, the standing wave, and the spectral triple approach to RH.

> **See [`LEDGER.md`](./LEDGER.md)** for an honest audit: axiom status, genuine theorem vs structural analogy, and complete structure vs curated bundle. No result here proves or materially advances any Millennium problem; this arc formalizes the elementary skeleton (Euler product ↔ CRT, Möbius inversion, order→topology, inverse-limit completions).

`WalshHadamard.v` is the Fourier transform native to the framework's xor/Fano structure: the Walsh–Hadamard transform on the Boolean cube F₂³. It is the first *actual* transform in the repo (a linear operator, not a comment), and proves — axiom-free — that it is symmetric, a self-inverse-up-to-scale involution (H² = 8·I), turns translation into a ±1 character sign, turns xor-convolution into a pointwise product, and realises the `StandingWave.v` apex reflection as a Fourier sign flip whose ±1 eigenspaces are the nodes and antinodes.

`WalshSampling.v` adds the **finite Shannon–Nyquist sampling theorem** on `F₂³` and makes explicit the *bridge* between the Fourier world and the "discrete contour integral." For the index-2 subgroup `H = {c2 = false}` (a hyperplane, `|H|=4`) and its annihilator `H⊥ = {c0=c1=false}` (`|H⊥|=2`): `WH_comb` is the **discrete Dirac comb** `Ĥ(1_H) = |H|·1_{H⊥}` (indicator of a subgroup transforms to indicator of its annihilator — the mechanism of all sampling theory); `poisson_summation` is **finite Poisson summation** `2·Σ_{x∈H} f = Σ_{y∈H⊥} Ĥf`; and `bandlimited_reconstruction` is the **discrete Nyquist criterion** — a signal band-limited off the `c2`-axis is constant along `H`-cosets, hence *reconstructed from its 4 samples on `H`* (`#samples = |H| = 4 =` bandwidth; undersampling would alias). **The bridge:** Poisson summation is the *same* identity whose continuous limit gives the theta/zeta functional equation — the `s ↔ 1−s` reflection the contour integral exploits; and `WH_involution` (recover `f` from `Ĥf`) + `bandlimited_reconstruction` (recover `f` from samples) are the **finite instances of the Fourier/Mellin inversion** that *is* Perron's contour formula (recover a summatory function from its Dirichlet series), with `PosetMobiusFTC` (Möbius inversion = discrete FTC) as the arithmetic-side cousin. So the shared **inversion / harmonic-duality principle** — sampling, Poisson summation, Möbius inversion, Perron — is here made concrete and machine-checked in the one setting needing no complex analysis: the self-dual finite group `F₂³`. **Axiom-free** ("Closed under the global context"). Honest scope: no rigorous `Ĥ → ζ` theorem is claimed — the bridge is the common inversion structure, provable here, out of reach for `ζ` without the contour.

`WalshUncertainty.v` proves the **discrete uncertainty principle** (Donoho–Stark) on `F₂³` — the harmonic-analytic *complement* of `WalshSampling`: a nonzero signal and its Walsh–Hadamard transform cannot **both** be concentrated. `discrete_uncertainty`: for `f ≠ 0`, `|supp f|·|supp(Ĥf)| ≥ 8 = |F₂³|` — the sharper `f` is localized in "space" (small support), the more its spectrum must spread, their support sizes multiplying to at least the group size. This is the finite, self-dual-group form of Heisenberg uncertainty, and it says *why* sampling can do no better: a signal both time- and band-limited past this product bound does not exist. Proof is the classical l¹/l∞ argument — `‖Ĥf‖∞ ≤ ‖f‖₁` (from `|χ|=1` + triangle inequality) and `‖f‖₁ ≤ |supp f|·‖f‖∞` (sum over the support), chained on `f` and on `Ĥf` with the inversion `Ĥ(Ĥf)=8f` (so `8‖f‖∞ = ‖Ĥ(Ĥf)‖∞`), then cancel the strictly positive `‖f‖∞` — carried out **entirely over `ℤ`** (signals are `ℤ`-valued), so it needs **no real analysis and no axioms** ("Closed under the global context").

`StandingWaveSpectrum.v` closes the loop: on the observer square F₂², it builds the amplitude signal directly from `StandingWave.v`'s own `fwd_*`/`inf_*` counts (proving it equals (−1, 0, 0, +1)), shows the apex reflection negates it (a −1-eigenvector), and concludes — both structurally via the shift theorem and by computation — that its Walsh spectrum is `(0, −2, −2, 0)`: exactly zero on the node frequencies and carrying all its weight on the antinodes. StandingWave's hand-counted table is thus the eigen-spectrum of a real Fourier transform.

`HeatFlow.v` runs the transform forwards in time: it puts a lazy bit-flip **diffusion (heat) step** on real signals over the cube F₂³ and proves the Walsh–Hadamard transform **diagonalizes** it — `WHr (step f) y = μ(y)·WHr f y` with `μ(y) = 1 − |y|/3 ∈ {1, ⅔, ⅓, 0}` — so `k` heat steps raise each eigenvalue to the `k`-th power. The DC (spatial-mean) mode is stationary (`μ = 1`, conserved), every other mode contracts (`μ ≤ ⅔`, spectral gap ⅓), and `iterate k f → equilib f` (the flat signal at the spatial mean): the deviation is exact-zero on DC and crushed by `(2/3)ᵏ` on every other mode. This is the rigorous form of "the temperature falls out of a static superposition." Standard `Reals` axioms only; no `admit`.

`MassGap.v` makes the "gap = mass gap" identification precise. Reading a Walsh mode `χ_y` as a state of energy `|y|` (number of flipped bits = quanta), it proves the bridge `μ(y) = 1 − |y|/3` (so `HeatFlow`'s decay eigenvalue is set by the energy), that the vacuum (`y=0`) is massless and stationary, and that the lightest excitation (`|y|=1`, `μ=2/3`, the slowest-decaying transient) has energy 1. That single number `1` is then shown to be simultaneously the `EigenSystem.spectral_gap` of the energy spectrum, the `physics.has_mass_gap` of the excitation spectrum, and — via `E=mc²` at `c=1` — the rest mass of the lightest excitation (`gap_is_mass_gap`). The nat statements are axiom-free (`cube_spectral_gap` is "Closed under the global context"); the bundled master theorem uses the standard `Reals` axioms only for the `μ` bridge.

`Involution.v` is the honest form of "no involution is free." A reversible (orthogonal) involution is lossless — we proved `Rop` orthogonal — so that slogan is *false* as an axiom and is **not** assumed. What is true and proved is the **residue decomposition**: every `f = sym f + anti f`, where `sym f = (f+Rop f)/2` is `Rop`-fixed (the free, conserved part) and `anti f = (f−Rop f)/2` is `Rop`-negated (the residue, a −1-eigenvector); the residue vanishes exactly on fixed points, the two sectors are orthogonal, and the reflection is not globally free (the standing-wave amplitude is a *pure* residue). The residue is precisely the antisymmetric/antinode part a diffusion dissipates — the residue is the seed of the flow. Standard `Reals` axioms only; no `admit`.

`Landauer.v` charges the cost of the involution to the diffusion. It puts a lazy bit-flip diffusion `stepQ` on the square (`Rop`'s space) and proves the residue is an exact ½-eigen-sector — `stepQ (anti f) = ½·anti f` — because `e0 ⊕ e1 = DIAG` makes the two neighbours `Rop`-conjugate and they cancel. Defining dissipated heat as lost L²-energy `⟨g,g⟩ − ⟨stepQ g, stepQ g⟩`, it shows `heat_dissipated (anti f) = ¾·‖anti f‖² ≥ 0`, strictly `> 0` whenever the residue is nonzero, and `= 0` exactly when `f` was already `Rop`-fixed. This is the honest "no involution is free" for the *irreversible reset*: erasing the residue always costs strictly positive energy (Landauer's structure, not `kT ln 2` — there's no temperature in the model; the cost is charged to the diffusion, not the free involution). Standard `Reals` axioms only; no `admit`.

`LandauerBound.v` is the genuine `kT ln 2` — the information/thermodynamics accounting of the same erasure. It defines the Bernoulli (Shannon) entropy `Hb` and proves, as pure theorems, that a fair bit has entropy `ln 2` (`Hb (1/2) = ln 2`), a deterministic bit has entropy 0, and so erasing a fair bit drops entropy by exactly `ln 2`. With `k, T > 0` as section variables (no physical constant added to the base), the model's minimum dissipated heat `k·T·ΔH` for that erasure is exactly `k·T·ln 2`, strictly positive; the inequality form takes the second law (`Q ≥ kT·ΔS`) as an *explicit hypothesis*, not an axiom, and yields `Q ≥ kT ln 2`. So the `ln 2` is proved and the `kT` is the carried physical conversion. This is the textbook Landauer floor for the one bit of information in the involution residue. Standard classical-`Reals` axioms only; no `admit`.

`BitDensity.v` builds the **bit density of states** and its symmetry about ½. On the n-bit cube a state's energy is its Hamming weight, and the number of states at energy `k` is the binomial `C(n,k)`. It proves (pure nat, axiom-free) that this density is **symmetric under the complement involution `k → n−k`** (`dos_symmetric`: `C(n,k)=C(n,n−k)`), that the complement is an involution whose fixed point is the half-weight `n/2`, and that the 3-bit density is the row `[1;3;3;1]` (summing to `2³`, matching `MassGap.excite`'s multiplicities). This is the discrete shadow of the critical line: the bit density is organized by the same complement/functional-equation involution about ½ as zeta's `s → 1−s`. **Honest scope:** an *analogy*, not a theorem about actual zeta zeros — the repo has no real zeta function, no zero-counting `N(T)~(T/2π)ln(T/2π)`, and no GUE spacing; only the shared symmetry-about-½ is formalized. Axiom-free ("Closed under the global context").

`BitCountContrast.v` formalizes the growth-class contrast between the bit count and the zeta zero count. The cumulative bit count `N_bit(n,E) = Σ_{k≤E} C(n,k)` (integrated density of states) is proven monotone, **saturating** (constant once `E ≥ n`), hence **bounded** by the total `N_bit n n = 2ⁿ` and therefore **not unbounded** — so it cannot equal any unbounded counting function, and cannot even match linear growth. The Riemann zero count obeys Riemann–von Mangoldt `N(T) ~ (T/2π)ln(T/2π)`, which is unbounded and superlinear. So the two live in **different growth classes**: the bit count is eventually constant; the zeta count grows without bound. Honest negative result — we formalize the boundedness contrast (pure nat, axiom-free), not the analytic `N(T)` (which isn't in the repo); the mismatch theorem takes "unbounded g" as its premise, which the zeta count provably satisfies (infinitely many zeros).

`WalshHadamardHilbertQ.v` is the **rational (ℚ) pilot** for dropping the Reals axioms. It re-proves the entire `WalshHadamardHilbert` development — positive-definite inner product, self-adjoint `WHq`, Parseval (`⟨WHq f, WHq g⟩ = 4⟨f,g⟩`), the unitary involution `Uq = ½WHq`, the self-adjoint apex reflection, and orthogonal ±1 eigenspaces — over `QArith` instead of `Reals`. Every constant in the construction is rational (`±1, ½, ¼, 2, 4`), so `Print Assumptions` reports **"Closed under the global context"**: no `ClassicalDedekindReals`, no `functional_extensionality`, no axioms at all. The ℚ tax is that equality is `Qeq` (`==`); the one subtlety is keeping the `½` rational constant away from `cbn` (the ½-normalized lemmas are proved via `WHq` scaling lemmas + `ring`). This is the template for re-basing the rest of the chain off ℝ.

`InvolutionQ.v` and `LandauerQ.v` continue the ℚ sweep on top of `WalshHadamardHilbertQ`: they re-prove `Involution` (residue decomposition `f = symQ f + antiQ f`, residue vanishes iff fixed, sectors orthogonal, not-globally-free) and `Landauer` (residue is an exact ½-eigen-sector of the diffusion, `heat_dissipated (antiQ f) = ¾·⟨antiQ f, antiQ f⟩ ≥ 0`, `> 0` iff the residue is nonzero, `= 0` iff `f` is fixed) entirely over `QArith`. Both report `Print Assumptions` **"Closed under the global context"** — the same theorems as the ℝ files, now with no Reals axioms and no `admit`. (The ℝ originals stay in place for the existing chain.)

`BiView.v` introduces a new object: **one arithmetic thing, two views**, as a **Galois connection** — the local–global (adelic) duality done in pure order theory, so **no topology is needed** (a Galois connection is just two posets + monotone adjoint maps; the p-adic/archimedean completeness the full adeles need is deliberately routed around, and isn't available in the stdlib anyway). The global view is `(Z,≤)` (a prime's valuation/divisibility coordinate), the local view is `(Q,≤)`, adjoined by `alpha = inject_Z` and `gamma = Qfloor` with `galois_connection : z ≤ gamma q ↔ alpha z ≤ q` (division-free, reusing stdlib `Qfloor_le`/`Qfloor_resp_le`/`Qfloor_Z`), plus monotonicity and the retract `gamma (alpha z) = z`. At resolution `n`, the density `dens z = z/n ∈ [0,1]` carries the **shared involution**: the global complement `z ↦ n−z` (BitDensity's `k↦n−k`) maps under `dens` to the local reflection `q ↦ 1−q` (RiemannHypothesisSpectral's `s↦1−s`), `shared_reflection : dens (n−z) == 1 − dens z` — literally the *same reflection about the centre seen in both views*, fixed at ½ / n⁄2. Local coordinate over ℚ; `Print Assumptions` **"Closed under the global context"** — no Reals axioms, no topology, no `admit`.

`BiViewProduct.v` takes the **finite-prime product** of `BiView`'s connection: over a set `Idx` of primes, the global view becomes the exponent/valuation **vector** `Idx → Z` whose product (componentwise) order **is the divisibility order** — with componentwise `min`/`max` = **gcd/lcm**, the genuine divisibility lattice (`gmeet_glb`, `gjoin_lub`). A product of Galois connections is a Galois connection, so `galois_product : gle f (gammaP g) ↔ lle (alphaP f) g` and monotonicity/retract all lift pointwise from `BiView`. The shared reflection lifts per‑prime (independent resolution `nv i` at each place): `shared_reflection_product`. Everything is a pointwise lift, so it stays axiom‑free — `Print Assumptions` **"Closed under the global context"** (no Reals, no topology). This is the divisibility‑lattice ↔ local‑coordinate adjunction at every place at once — the order‑theoretic skeleton of the adele.

`HeatFlowQ.v` and `MassGapQ.v` finish the ℚ sweep for the diffusion side. `HeatFlowQ` re-proves the Walsh diagonalization of the lazy bit-flip diffusion over `QArith` — `step_diagonal : WHq (step f) y == μ(y)·WHq f y` (proved via `WHq` linearity + the shift theorem, keeping the `½`/`⅙` constants away from `cbn`), `iterate_spectrum` (`μ(y)ᵏ`), `mu_zero`, `mu_bound` (`0 ≤ μ ≤ ⅔`), and `dc_preserved` — all axiom-free. `MassGapQ` gives the μ‑energy bridge over ℚ: `mu_spectrum` (`μ` = `1, ⅔, ⅓, 0` on the four Hamming shells = `1−|y|/3`), vacuum stationary, lightest excitation `⅔`. Both `Print Assumptions` **"Closed under the global context"**. (The one genuinely transcendental holdout is `LandauerBound`'s `ln 2`.)

`ProductFormula.v` closes the **product formula** as an order/algebra identity on `BiViewProduct`'s divisibility lattice — topology-free. Over a finite list of places, the degree (total valuation) `deg L f = Σ f i` satisfies `deg_gcd_lcm : deg (gcd) + deg (lcm) = deg a + deg b` — the additive/valuation form of `gcd(a,b)·lcm(a,b) = a·b` (per place, `min + max = a + b`), the multiplicative product formula of the divisibility lattice; and `deg_reflection : deg (complement f) = total − deg f`, so the shared reflection acts on the degree as `d ↦ total − d` (fixed at `total/2`). Pure `Z`/`lia`, axiom-free.

`LandauerBoundL.v` removes the *last* Reals dependency by abstracting the one‑bit entropy to a **parameter `L`**. `LandauerBound.v` proved the genuinely transcendental `Hb(½) = ln 2` (which forces the classical Reals axioms); here we don't compute the entropy — we take `L : Q`, `0 < L` (the entropy of one fair bit, `= ln 2` in the ℝ interpretation, never committed to). Then the whole Landauer structure is pure ℚ: `landauer_min dS = k·T·dS`, `erase_bit_heat = k·T·L`, additivity over bits (`landauer_min (a+b) = landauer_min a + landauer_min b`), monotonicity, strict positivity, and the second‑law inequality — all axiom‑free (`Print Assumptions` **"Closed under the global context"**). So the transcendental `ln 2` is quarantined to a labelled parameter, and every construction in this directory now has an axiom‑free ℚ realization; only `LandauerBound.v` itself (which deliberately keeps the real `ln 2`) uses the Reals axioms.

`MobiusReciprocal.v` builds the **Möbius / inversion side** of the primon-gas bridge — the *reciprocal* of the Euler factor — over ℚ, axiom-free. `PrimonGas` gave the ζ side (`psum → 1/(1−x)`); its reciprocal `(1 − x)` is *exactly* the per-mode **Möbius Dirichlet series** `Σ_k μ(pᵏ) xᵏ`, a finite series because `μ(1)=1, μ(p)=−1, μ(p^{k≥2})=0`. So `mumode x = 1 − x` (`mumode_is_museries` proves it equals the truncated series `Σ_k μ(pᵏ)xᵏ` for `K≥2`, the two-term collapse). The per-mode **Dirichlet convolution** `mu_zeta_id : mumode x · psum x K = 1 − xᴷ` is exactly `PrimonGas.geom_closed` reinterpreted — the truncated `ζ ⋆ μ`, whose RHS `→ 1 = δ` as `K→∞`, i.e. Möbius inversion. And `mu_zeta_product` gives the **multi-prime reciprocal Euler product**: `∏ₚ mumode(xₚ) · ∏ₚ(Euler factor) = ∏ₚ(1 − xₚᴷ) → 1`, so `∏ₚ(1 − p⁻ˢ) = 1/ζ(s) = Σₙ μ(n) n⁻ˢ`. `Print Assumptions mobius_reciprocal` → **"Closed under the global context"** (pure ℚ). This is the adjoint we'd skipped: the bridge was complete on the ζ / integration / creation side but never on the Möbius / inversion side, even though `(1−x)` was already sitting in `geom_closed`.

`MobiusReciprocalR.v` is the **analytic reciprocal** (over ℝ, quarantined axioms): the Möbius factor `(1 − x)` is the exact multiplicative inverse of the Euler factor `1/(1−x)` (`mu_inverse`, via `Rinv_r`), so `mobius_zeta_delta` combines `EulerFactorR.geom_limit` (`Σ xᵏ → 1/(1−x)`) with `(1−x)·(1/(1−x)) = 1` — the analytic `ζ ⋆ μ = δ` per mode. Uses the classical Reals axioms (quarantined, like `EulerFactorR.v`); the ℚ core stays "Closed under the global context".

`PosetTopology.v` answers "can topology fall out of two posets and a map operator between them?" — **yes, axiom-free, from pure order** (no metric, no limits, no analysis library — which is what we found stdlib lacks). Three layers. **(1) Alexandrov topology**: on a single preorder, the opens are the **up-sets** (`Op`); they form a genuine topology — `open_empty`, `open_full`, `open_inter` (finite intersections), `open_union` (arbitrary unions) — and the **specialization order recovers `≤`** exactly (`specialization : leX x y ↔ ∀ open U, U x → U y`). So topology falls out of one poset. **(2) The map operator**: a **monotone** map `f` between two posets is automatically **continuous** (`continuous_of_monotone`: preimage of an up-set is an up-set). **(3) Two posets + a Galois connection** (the adjoint pair of map operators — *exactly* `BiView`'s `alpha`/`gamma`): the composite `cl = γ∘α` is a **closure operator** — extensive (`cl_extensive`), monotone (`cl_monotone`), idempotent (`cl_idempotent`) — whose fixed points are the Galois-**closed** elements, the **overlap** where the two posets' views coincide, forming the closed sets (a closure system). `Print Assumptions poset_topology` → **"Closed under the global context"**. Instances: `divisibility_up_open` shows `FreeDivMeet.fle`'s principal up-sets (multiples of `a`) are open (the divisibility lattice as an Alexandrov space); `BiView.galois_connection` is a Galois connection so its `γ∘α` is a closure operator. **Honest scope:** the Alexandrov part is a genuine topology; the Galois part is a closure *operator/system* (a Galois closure need not preserve finite unions, so it is the order-theoretic skeleton of closed sets, not automatically a topological closure).

`FiniteTopology.v` pins `PosetTopology` down to a **concrete continuous map between two finite topologies**, axiom-free. Two finite spaces — `two = {L,R}` with `L ≤ R` (Sierpiński: opens `∅,{R},all`) and the 3-chain `three = {A,B,C}` (`A≤B≤C`) — with finite carrier enumerations. A concrete monotone map `f : three → two` (`A,B ↦ L`, `C ↦ R`) is **continuous** (`f_continuous`, exactly `PosetTopology.continuous_of_monotone` instantiated), verified concretely by `preim_R_is_C`: the preimage of the open `{R}` is the open `{C}`. `not_open_L` shows `{L}` is *not* open, so the topology is genuinely coarser than discrete. Key fact used: on a finite set every topology is Alexandrov (finite ∩ = arbitrary ∩), so finite T₀ spaces ≅ finite posets — the poset machinery captures *all* finite topologies; only finite carriers + an instance are added here. `Print Assumptions finite_topology` → **"Closed under the global context"**.

`ChainTower.v` extends this to an **infinite family of finite topologies** connected by continuous maps, axiom-free. For each `n`, the finite chain `Chain n = {k | k≤n}` is a finite Alexandrov space (`leC_refl`/`leC_trans`; `chain_finite` witnesses the finite carrier via `seq 0 (S n)`). The **truncation bonding maps** `trunc : Chain(n+1) → Chain n` (`k ↦ min k n`) are monotone hence continuous (`trunc_continuous`) — an infinite **inverse system** of finite topologies; **dynamic sizing** is captured by the **growing inclusion maps** `incl : Chain n → Chain(n+1)` (`incl_continuous`), a directed system whose colimit is the unbounded chain ℕ. `Chain n` is exactly `Div(pⁿ)` (the finite divisor lattice), so this is the divisor-lattice tower: its **inverse limit is `ω+1 = ℕ ∪ {∞}`** (the profinite completion of the chain, built in `InvLimit.v`) and its direct limit is ℕ — both infinite, marking the boundary where the family stops being finite. (`ℤ_p` is the inverse limit of the *different* tower `ℤ/pⁿ` under mod‑`pⁿ` reduction — ring quotients, not order truncations.) `Print Assumptions chain_tower` → **"Closed under the global context"**.

`PadicIntegers.v` builds **the p‑adic integers `ℤ_p` as the inverse limit of the genuine tower `ℤ/pⁿ`** (mod‑`pⁿ` reduction maps), axiom-free over `nat` — the completion `ChainTower`'s order‑truncation tower does *not* give. Same coherent‑sequence recipe as `InvLimit`, but the levels are the residue rings and the bonding maps are ring quotients: `Zp = { a : ℕ→ℕ | ∀ n, a n = a(n+1) mod pⁿ }` (`redcoh`). The reduction projection cone commutes with the bonding maps (`projZ_cone`); the **ring operations** `rzero`/`rone`/`radd`/`rmul` (reduced mod `pⁿ` at each level) **descend to the inverse limit** — `redcoh_add`, `redcoh_mul` prove coherence is preserved, via the nested‑modulus lemma `nested_mod` (`(x mod p^{n+1}) mod pⁿ = x mod pⁿ`, from `Nat.Div0.mod_mul_r`); the **projections are ring homomorphisms** (`projZ_add`, `projZ_mul`); and the **universal property** `Zmediate` (coherent residue data assembles into a limit point — this *is* completeness: a coherent Cauchy tower has a limit). `Print Assumptions padic_integers` → **"Closed under the global context"**. This is the algebraic completion; `ℤ_p` as inverse limit.

`PadicRing.v` **consolidates** the algebra of `ℤ_p`: under pointwise equality `Zeq`, the p‑adic integers form a **commutative semiring** — `Zp_comm_semiring` bundles associativity/commutativity of `+`/`*`, the unit laws (`Zadd_0_l`, `Zmul_1_l`, via `redcoh_lt`: a coherent digit is a genuine residue `< pⁿ`), `Zmul_0_l`, and left distributivity, each reducing to `ℤ/pⁿ` via the `Nat.Div0` idempotent‑mod lemmas. `Print Assumptions Zp_comm_semiring` → **"Closed under the global context"**. **Honest note:** `ℤ_p` is a full commutative *ring* (additive inverses exist, `−a = (pⁿ−a) mod pⁿ`), but the coherence of that negation across levels (nat truncated subtraction) and Coq `Add Ring` registration are **deferred** — see `LEDGER.md`.

`PadicRingOpp.v` closes the ℤ_p **ring** gap `PadicRing` deferred: the **additive inverse**, upgrading the commutative semiring to a full commutative ring. Negation is `(−a)ₙ = (pⁿ − aₙ) mod pⁿ`; the subtlety is coherence across levels despite nat *truncated* subtraction — `neg_coh` proves `(pⁿ−aₙ) mod pⁿ = (pⁿ⁺¹−aₙ₊₁) mod pⁿ` via the div/mod decomposition `aₙ₊₁ = pⁿ·k + aₙ` (`k<p`), so `pⁿ⁺¹−aₙ₊₁ = (pⁿ−aₙ) + (p−k−1)pⁿ` reduces to the same residue (`nia`). Then `Zadd_opp_l : (−X)+X = 0`. Combined with `PadicRing.Zp_comm_semiring`, `ℤ_p` is a **commutative ring**. `Print Assumptions Zadd_opp_l` → **"Closed under the global context"**. (Coq `Add Ring` *registration* — setoid Proper boilerplate — is still not done, but the ring axioms are all proved.)

`PadicMetric.v` builds **the metric side of `ℤ_p`** — the same object seen through `Padic`'s **ultrametric** — axiom-free (pure ℚ, no Reals). Residue sequences are close when they agree on leading levels: `agree_upto a b k` (⟺ `|a−b|_p ≤ prad k = pabs k = p⁻ᵏ`) is the p‑adic ball of radius `prad k`. It's a genuine **ultrametric**: reflexive, symmetric, radius‑monotone (`agree_mono`), and the **strong triangle inequality** `strong_triangle` — agreement to radii `k₁,k₂` gives agreement to `min(k₁,k₂)`, and via `Padic.pabs_min` the radius `prad(min k₁ k₂) = max(prad k₁, prad k₂)`, so `d(a,c) ≤ max(d(a,b),d(b,c))`. Plus `separated` (agreement at every level ⇒ equality: a genuine Hausdorff metric) and `prad_pos` (radii `> 0`, `→ 0`). `Print Assumptions padic_metric` → **"Closed under the global context"**. Together with `PadicIntegers` this exhibits `ℤ_p` as the p‑adic **metric completion**: the algebraic inverse limit and the metric completion are the *same* object (radii `pabs k → 0`; coherent Cauchy data has a limit via `Zmediate`).

`InvLimit.v` builds **the limit object** of the `ChainTower`, axiom-free, as **coherent sequences** — no completion, no classical axioms, just a dependent function plus a `Prop` compatibility condition. A point of `InvLim` is a choice `x_n : Chain n` at every level with `val (trunc x_{n+1}) = val x_n` (`Coherent`). It carries the pointwise order `leL` (hence its Alexandrov topology via `PosetTopology`), a **continuous projection cone** `projL : InvLim → Chain n` that commutes with the truncation bonding maps (`projL_cone`, `projL_continuous`), and the **universal property** `mediate`: every compatible cone `Z → Chain n` factors through `InvLim` with `projL n ∘ mediate = famₙ`. Its shape is characterised as **`ω+1`**: a top point `infty` (the diagonal `x_n = n`, `infty_top`) plus an embedding of every natural number (`emb m`, the capped `min m n`). `Print Assumptions inverse_limit` → **"Closed under the global context"**. So the genuine limit object of an infinite tower of finite topologies is fully constructible from pure order — the completion‑free route.

`ProductMobius.v` lifts **Möbius and von Mangoldt to the 2‑prime product lattice** (divisors of `pᵃqᵇ`), axiom-free over ℚ. **Möbius/reciprocal:** the Möbius function is *multiplicative* (`mu2 a b = μ(pᵃ)·μ(qᵇ)`), so the 2D reciprocal‑Euler series factors (`museries2_factor`, via a separable‑double‑sum lemma `double_sum_sep`) and for `K≥2` collapses to `mu2_reciprocal : Σ_{a,b<K} μ(pᵃ)μ(qᵇ) xᵃyᵇ = (1−x)(1−y)` — the product of two reciprocal Euler factors. **Von Mangoldt:** on the product, Λ is the 2D Möbius transform `M2` (from `ProductFTC`) of the additive log `Lsum(i,j)=i·Lp+j·Lq`; it is **supported on the prime‑power axes** — `vm2_axis_p : M2 Lsum (m+1) 0 = Lp`, `vm2_axis_q : M2 Lsum 0 (n+1) = Lq`, `vm2_origin = 0`, and the headline `vm2_interior_zero : M2 Lsum (m+1) (n+1) = 0` (Λ vanishes on genuine products `pᵃqᵇ`, `a,b≥1`, since those aren't prime powers). `Print Assumptions product_mobius` → **"Closed under the global context"**.

`ProductFTC.v` fills the **single‑prime → product‑lattice** gap for the FTC: the **2D product‑of‑chains Möbius inversion** on ℕ×ℕ (divisors of `pᵐqⁿ` = a product of two chains), axiom-free over ℚ. The 2D transforms are the compositions of `PosetMobiusFTC`'s 1D ones in each coordinate — `Z2 f m n = Σ_{i≤m}Σ_{j≤n} f i j` (double divisor sum) and `M2 g` (double backward difference) — and they are mutually inverse: `ftc2_1 : M2 (Z2 f) = f`, `ftc2_2 : Z2 (M2 g) = g`. The one new ingredient is that the 1D transforms **commute across independent coordinates** (`mobius_zeta_comm`, `zeta_mobius_comm` — `mobius_t` is linear, `zeta_t` a finite sum, using `zeta_t_sub`/`_ext`, `mobius_t_ext`); each 2D FTC is then two applications of the 1D FTC (Fubini‑style). `Print Assumptions product_ftc` → **"Closed under the global context"**. (Generalises to `k` primes by iterating; the full arbitrary‑ℕ⁺ divisor‑sum form still needs divisor enumeration — `LEDGER.md`.)

`PosetMobiusFTC.v` builds the **poset Möbius inversion = discrete Fundamental Theorem of Calculus** on the divisibility lattice, over ℚ, axiom-free — the third and last Möbius thread. On the single-prime divisibility lattice (the divisors of `pᵐ` form a **chain** `[0..m]` under divisibility, indexed by the exponent) the two adjoint operations are the **zeta transform** `Z[f](n) = Σ_{j=0}^n f(j)` (divisor sum = discrete *integration*, `zeta_t`) and the **Möbius transform** `M[g](n) = g(n) − g(n−1)` (backward difference = discrete *differentiation*, `mobius_t`). They are **mutually inverse** — the discrete FTC, which *is* Möbius inversion: `ftc_1 : M[Z[f]] = f` (differentiate the integral) and `ftc_2 : Z[M[g]] = g` (integrate the derivative — telescoping). And `mobius_t_is_mu_conv` identifies `M` with the poset‑Möbius convolution `Σ_{d∣n} μ(n/d) g(d)` (reusing `VonMangoldt.dconv`/`dconv_mu`, with `μ = MobiusReciprocal.mu_pp`). `Print Assumptions poset_mobius_ftc` → **"Closed under the global context"**. This is the single‑prime chain; the full lattice (`FreeDivMeet`) is a product of chains so the inversion extends coordinatewise, and the fully general ℕ⁺ divisor‑sum form (needing divisor enumeration) is deferred.

`VonMangoldt.v` builds the **von Mangoldt function and its Möbius inversion**, over ℚ, axiom-free — the arithmetic heart behind the logarithmic derivative of the Euler product. For a single prime `p`, indexing arithmetic functions by the exponent `k` (so `f(pᵏ) = f k`) with the single-particle energy `L = log p` kept as a formal parameter (à la `LandauerBoundL`): `Λ(pᵏ) = L` for `k≥1` else `0`, `log(pᵏ) = k·L`, `μ(pᵏ)` from `MobiusReciprocal.mu_pp`. The per-prime **Dirichlet convolution** is the Cauchy product on exponents (`dconv`). Headline: `vm_mobius_inversion : Λ = μ ⋆ log` (`dconv muf logf m = lamf m`) — **Möbius inversion**; its lemma `dconv_mu` shows `μ ⋆ (·)` is the **backward difference operator** (via `mu_weighted`, which generalises `MobiusReciprocal.museries_from2`), so `Λ` is the *discrete derivative of log*. And `vm_summatory : Σ_{d∣pᵐ} Λ(d) = log(pᵐ)`. `Print Assumptions von_mangoldt` → **"Closed under the global context"** (pure ℚ).

`VonMangoldtR.v` is the **analytic face** (over ℝ, quarantined axioms): the genuine **logarithmic derivative** of the Euler factor `Z_p(s) = 1/(1−p⁻ˢ)`. `vonmangoldt_logderiv` proves `d/ds log Z_p = −log p · fug/(1−fug)` (`fug = p⁻ˢ`), built by composing `derivable_pt_lim_ln` with `LadderDerivR.euler_factor_energy_deriv`. So `−d/ds log Z_p = log p · fug/(1−fug) = Σ_{k≥1} Λ(pᵏ) p⁻ᵏˢ` (since `Λ(pᵏ)=log p`); summed over primes this is the classical `−ζ'/ζ(s) = Σₙ Λ(n) n⁻ˢ`. This closes the loop with `LadderDerivR`'s energy `log p`: the per-prime logarithmic derivative *is* the von Mangoldt Dirichlet series. Uses the classical Reals axioms (quarantined); the ℚ core stays "Closed under the global context".

`NxnZero.v` proves the analysis lemma `LadderDerivR` flagged as missing: `n·xⁿ → 0` for `0 ≤ x < 1` (`nxn_zero`, `Un_cv (fun n => INR n * x^n) 0`) — not in stdlib. Via second-order Bernoulli `(1+h)ⁿ ≥ 1 + nh + n(n−1)/2·h²` (`ber2`, by induction) it bounds `n·xⁿ ≤ 2/((n−1)h²)` and squeezes against `2/h²·/(n−1) → 0` (`cv_infty_cv_0` + `INR_unbounded`). Building on it, `number_series` proves the full number-weighted series `Σ_{k=0}^∞ k·xᵏ = x/(1−x)²` (`Un_cv (nsumR x) (x/(1−x)^2)`) — completing `LadderDerivR`'s `deriv_number` at the series level — via the closed-form recurrence `(1−x)·S_N = G_{N+1} − 1 − (N+1)xᴺ⁺¹` (`key`) and the limits (`geom_limit`, `nxn_zero`, `CV_minus`/`CV_mult`).

`LadderDerivR.v` is the **ladder-derivative payoff** in the analytic layer (over ℝ): the number operator `N = x·d/dx` and the primon **energy `log p`** as *genuine* derivatives (`Ranalysis`), completing `LadderOps.v`. Where `LadderOps` proved the *formal/termwise* facts `MD` (`x·D(xᵏ) = N(xᵏ)`) and `deriv_number` (`x·dsum = nsum`) over ℚ, here they become analytic derivatives of the partition-function limit `Z(x) = 1/(1−x)`: `Zinf_deriv` (`d/dx(1/(1−x)) = 1/(1−x)²`, via stdlib `derivable_pt_lim_div`) and `number_operator` (`x·d/dx Z = x/(1−x)²`, the mean-occupation generating function). The **energy** payoff: `fugacity_deriv` proves `d/ds p⁻ˢ = −log p · p⁻ˢ` — differentiating the fugacity `p⁻ˢ = exp(−s·ln p)` *brings down the single-particle energy* `log p` (via `derivable_pt_lim_comp` with `exp`); and `euler_factor_energy_deriv` composes it through `Zinf_deriv` to give `d/ds(1/(1−p⁻ˢ)) = −log p · p⁻ˢ/(1−p⁻ˢ)²`, so differentiating the Euler factor w.r.t. `s` pulls out `log p`. Like `EulerFactorR.v`, this file **uses the classical Reals axioms** (quarantined); `LadderOps`, `PrimonGas` and the rest of the ℚ core stay "Closed under the global context".

`PrimorialEuler.v` answers "can the Euler product over all primes be relativized to primorials?" — **yes**. It builds the **primorial tower**: `EP n = ∏_{i<n} 1/(1−pᵢ⁻²)`, the finite Euler product over the first `n` primes (the primes dividing the `n`-th primorial `primor n`), for an abstract prime enumeration `P` (`P i ≥ 2`; instantiate with the real primes for the genuine primorial). Proved: each rung adjoins one factor — `EP_rec : EP(n+1) = EP n · 1/(1−pₙ⁻²)` — exactly as the primorial grows `primor_rec : primor(n+1) = primor n · pₙ`; each rung **is a genuine finite Euler product** — `EP_is_limit : Un_cv (Zpartial (pfugs n)) (EP n)`, the `K→∞` product of truncated geometric sums = the primon-gas partition function *restricted to the first n primes* (from `EulerProductR`); and the tower is positive and **monotone increasing** (`EP_pos`, `EP_monotone`). So the infinite Euler product over all primes is the *limit of this primorial-indexed monotone tower*. `Print Assumptions primorial_euler` → the quarantined Reals axioms. **Honest scope:** the tower, its per-rung Euler-product identity, and monotonicity are proved; its *convergence* to `ζ(s)` (the top of the tower) still needs a uniform bound on the smooth partial sums, deferred.

`PrimorialEulerBound.v` answers "are the primorial rungs uniformly bounded?" — **yes**, and so the primorial‑relativized Euler product **converges**. Under the honest hypothesis `P i ≥ i+2` (true for the actual primes `2,3,5,…`), each Euler factor `1/(1−pᵢ⁻²)` is majorized by the consecutive‑integer factor `1/(1−1/(i+2)²)`, whose product **telescopes**: `M n = ∏_{i<n} 1/(1−1/(i+2)²) = 2 − 2/(n+2) < 2` (`M_closed`). Hence `EP_bounded : EP n ≤ 2` for all `n` — a **uniform bound** — and with `PrimorialEuler.EP_monotone`, monotone‑bounded convergence (`growing_cv`) gives `EP_converges : { l | Un_cv (EP P) l }`: **the Euler product over all primes, relativized to the primorial tower, exists as a limit.** `Print Assumptions primorial_euler_converges` → the quarantined Reals axioms. This closes the boundedness gap flagged in `PrimorialEuler` (still an *existence* result — the limit's value, and `ζ` as an infinite product identity `∏_p = Σ_n`, remain out of scope).

`PrimeFactorizationN.v` discharges the **injective (uniqueness) half** of the `PrimorialZeta` crux — **unique factorization over the first `n` primes, as a bijection** — generalizing `FreeDivMeetIso` from 2 primes to a list `ps` of `n` distinct primes. The coding map `code ps ks = ∏ᵢ psᵢ^{ksᵢ}` (occupation/exponent tuple → the `{ps}`-smooth number it factors) is proven to be an **order embedding** (`code_order_iso_N`: `code ps as ∣ code ps bs ↔ Forall2 (≤) as bs` — divisibility of coded numbers = pointwise order of exponent tuples) and hence **injective** (`code_inj`: `code ps as = code ps bs → as = bs`, the exponent tuple is *unique*). So `code` is a bijection of the first-`n`-primes exponent tuples onto the smooth numbers they generate. The engine is `prime_pow_cancel`: from `p^a·X = p^b·Y` with `X,Y` coprime to a prime `p`, the `p`-adic valuation forces `a=b` and `X=Y` — peeling one prime at a time by the exact `Gauss` + `rp_pow_gen` argument of `FreeDivMeetIso`, iterated down a `NoDup` prime list. **Axiom-free** ("Closed under the global context"). This is exactly the "unique" of unique factorization; the remaining half of the crux is *existence* (surjectivity onto **all** `{ps}`-smooth numbers, i.e. every such number *has* a factorization).

`PrimeFactorizationExists.v` discharges the **surjective (existence) half** — closing the crux: `code ps` is a genuine **bijection** between length-`n` exponent tuples and the `{ps}`-smooth positive numbers. `code_surj` proves every `m>0` whose prime divisors all lie in `ps` *has* an exponent tuple `ks` with `code ps ks = m`. Two from-scratch engines, both axiom-free: `padic_val` — the **p-adic valuation is total** (every `m>0` splits as `p^v·m'` with `p ∤ m'`), by well-founded recursion on `0 ≤ · < m` (`Z.lt_wf 0`), peeling one factor of `p` at a time; and `has_prime_divisor` — every `m>1` has a prime divisor, by well-founded recursion via stdlib's `not_prime_divide`. Surjectivity then peels `p₀, p₁, …` down the `NoDup` prime list: extract the `p`-valuation `m = p^a·m₁`, show `m₁` is `{ps'}`-smooth (its prime factors divide `m` hence lie in `ps`, and `p ∤ m₁` rules out `p`), recurse. The master `code_bijection_smooth` bundles this surjectivity with `PrimeFactorizationN.code_inj` (injectivity) — **both halves of the factorization crux, now complete and fully axiom-free** ("Closed under the global context").

`ProductFormulaQ.v` is the **product formula over ℚ** — the adelic bridge, stated at the one level where it costs *no axioms*: the rational level. Over ℚ the places are the archimedean `∞` and one non-archimedean place per prime `p`, and Ostrowski's product formula says `∏_v |x|_v = 1`, i.e. `|x|_∞ = 1/∏_p |x|_p` — **the archimedean size of a rational is reconstructed from its finitely many p-adic valuations**. `product_formula_int`: for every positive integer `n`, there is a prime list + exponent tuple (via `code_surj` over `primes_upto`) with `code ps ks = n` (that's `n = ∏_p p^{v_p(n)}`) and `inject_Z n · fabs ps ks == 1`, where `fabs = ∏_p p^{−v_p}` is the product of the non-archimedean absolute values. `product_formula_Q`: for a positive rational `x = a/b` written over a common prime list, `(|x|_∞)·(∏_p |x|_p) == 1` — the quotient of the two integer instances (`a/b · b/a = 1`). The whole file is **axiom-free** ("Closed under the global context") precisely *because* for a rational input the archimedean absolute value is itself a rational (here a ratio of two coded integers); no real numbers are constructed. **This is the honest answer to whether the profinite/adelic completion bridges to ℝ:** for rational points it does — finite valuations determine archimedean size, provable in ℚ — but it does **not** build `ℝ = ℚ_∞`, which is the *completion* at the archimedean place (a separate factor `𝔸_ℚ = ℝ × 𝔸_f`, orthogonal to the finite/profinite tower `∏_p ℤ_p = lim ℤ/nℤ`). The closing note pins exactly where the quarantined Reals axioms would re-enter: the Cauchy/Dedekind completion to ℝ, hence Γ-factors, the completed `ξ(s)`, the functional equation, and ζ special values (π²/6) — all archimedean-local and out of reach of this bridge.

`ProfiniteCRT.v` makes the **profinite side explicit** — the other half of the adelic picture from `ProductFormulaQ`. The profinite completion `Ẑ = lim_n ℤ/nℤ` (over `n` ordered by divisibility) is, by CRT, the product over primes `Ẑ = ∏_p ℤ_p` with `ℤ_p = lim_k ℤ/pᵏℤ` — the finite/non-archimedean factor `𝔸_f` of the adele ring `𝔸_ℚ = ℝ × 𝔸_f`, and (unlike ℝ) built entirely from finite discrete data, hence **axiom-free**. Two ingredients, machine-checked: **(1)** the **CRT ring isomorphism** `ℤ/mnℤ ≅ ℤ/mℤ × ℤ/nℤ` for coprime `m,n` — `crt_iso` gives an *explicit* inverse to the reduction pair `x ↦ (x mod m, x mod n)` via Bézout reconstruction `recon a b = a·(vn) + b·(um)` (from `u·m+v·n=1`), with `crt_proj_m`/`crt_proj_n` (`recon ≡ a mod m`, `≡ b mod n`) and `crt_roundtrip` (`recon(x mod m, x mod n) ≡ x mod mn`, combining the two congruences by coprimality via `Gauss`); reduction is a ring homomorphism (`Zplus_mod`/`Zmult_mod`), so the bijection is a ring iso, and iterating over `n = ∏_p p^{v_p(n)}` yields the finite shadow `ℤ/nℤ ≅ ∏_p ℤ/p^{v_p}ℤ`. **(2)** the **inverse system** `lim_n ℤ/nℤ`: the projection maps commute (`proj_compat`: `(x mod n) mod d = x mod d` for `d | n`, via `Zmod_div_mod`), the prime-power tower `ℤ/p^{k+1} → ℤ/pᵏ` is an instance (`tower_proj`), and the index set is directed (`system_directed`: any two levels divide their product) — a cofiltered inverse system of finite rings, exactly the data of `Ẑ`. `profinite_crt` bundles both. **Axiom-free** ("Closed under the global context"); the archimedean factor `ℝ = ℚ_∞` remains the separate, irreducibly-real half this construction never reaches.

`LocalGlobalCompat.v` proves that the two descriptions of the finite adele factor `𝔸_f` — the **valuation side** (`ProductFormulaQ`'s `∏_p |x|_p`, `fabs`) and the **ring side** (`ProfiniteCRT`'s `Ẑ = lim ℤ/nℤ = ∏_p ℤ_p`) — read the *same* exponent data, the local–global principle on the non-archimedean side. **Global** (`fabs_is_recip`): `fabs ps ks == /inject_Z (code ps ks)` — the product of the local p-adic absolute values is `1/n`, the reciprocal of the global integer `n`. **Local, per prime** (`local_global_compat`): for `n = code (p::ps') (k::ks')`, the *ring* side splits `n = p^k · m` with `p^k` coprime to the cofactor `m = code ps' ks'`, so `crt_iso` gives `ℤ/nℤ ≅ ℤ/(p^k)ℤ × ℤ/mℤ` — the local ring component at `p` is `ℤ/(p^k)ℤ`, modulus `p^k`; and the *valuation* side has `fabs` peel off exactly `/inject_Z (p^k) = |n|_p`, the reciprocal of that **same** modulus. So the exponent `k = v_p(n)` is simultaneously the level of the local ring factor `ℤ/p^{v_p}ℤ` and the exponent of the local absolute value `|n|_p = p^{−v_p}`; iterating down the prime list, `Ẑ`'s decomposition `∏_p ℤ_p` proceeds in lockstep with `fabs`'s factorization `∏_p |·|_p`. The global object `n` and its profinite image carry the identical local data the product formula measures — **axiom-free** ("Closed under the global context"). `ℝ = ℚ_∞` is, as always, the separate archimedean factor outside this compatibility.

`ComplexField.v` defines a **custom complex field `C = ℝ[i] = ℝ × ℝ`** under the repo's axiom discipline — the controlled replacement for an opaque/external `C` (which would inject its own axioms and break the ledger). Rather than *assume* the complex-number axioms, we **prove every one of them as a theorem** over the repo's classical `ℝ`, so the axiom footprint is *exactly* the quarantined classical-`ℝ` axioms (`ClassicalDedekindReals.sig_forall_dec`, `functional_extensionality_dep`) — **no new axiom** is introduced by `C`. `complex_field_axioms` bundles: the commutative **field** laws (`C_ring_theory`/`C_field_theory`, both registered so `ring` and `field` work on `C`); `i² = −1` (`Ci_sq`); the 2-dimensionality `c = Re c + i·Im c` (`C_decompose`); `ℝ ↪ C` an injective **ring homomorphism** (`RtoC_add`/`RtoC_mul`/`RtoC_inj` — `ℝ` is a subfield); and **conjugation** an involution and ring hom with `c·c̄ = |c|² = Cnorm2 c ≥ 0` (`Cconj_involutive`/`Cconj_add`/`Cconj_mul`/`Cmul_conj`/`Cnorm2_nonneg`). `C` is the genuine field `ℝ[i]`: downstream work (roots of unity, a bona-fide DFT over `ℂ`, the analytic zeta side) can use `ring`/`field`, `i²=−1`, conjugation, and modulus **without importing any new axiom**. **Weak/strong layering of the real↔imaginary coupling** (`ring_core_generalization`): the only place `−1` enters the product is the `Im·Im` term, so we make it a parameter `δ` (the square of the generator, `i²=δ`) via `Cmulg δ`. The **ring core is weak** — `Cring_theory_g` proves `ℝ[e]/(e²=δ)` is a commutative ring for *every* real `δ` (the proof never mentions `i` or the value `−1`), our `Cmul` is exactly the `δ=−1` instance (`Cmul_is_gen`), and the generator squares to the parameter `e²=δ` (`Cig_sq`). So **`i²=−1` (`Ci_sq`) is a *downstream* `δ=−1` evaluation, not a foundation.** The **field cap is strong** — invertibility divides by the norm-form `Re²−δ·Im²`, nonzero off `0` *exactly* when `δ<0` (`Cnorm2g_pos`); that single hypothesis is the whole gap between "commutative ring for all `δ`" and "field", and our `C` takes `δ=−1<0`. The trichotomy by sign of `δ` is the three 2-D real algebras: **`δ<0` complex** (our field), **`δ=0` dual numbers** (`ε²=0`, nilpotent), **`δ>0` split-complex** (`j²=1`, zero divisors). Classical Reals axioms (quarantined, via `ℝ`).

`RootsOfUnity.v` builds the **N-th roots of unity** in that custom `C` and proves the **complex-DFT orthogonality** relation — the general-`N` cousin of `WalshHadamard`'s `H² = 8I`. Defining `w N := exp(2πi/N) = cos(2π/N) + i·sin(2π/N)` and complex powers/sums `Cpow`/`Csum`: `de_moivre` proves `(cos θ + i sin θ)ⁿ = cos(nθ) + i sin(nθ)` (induction via `cos_plus`/`sin_plus`); `w_pow_N` proves `(w N)ᴺ = 1` (De Moivre at `N·(2π/N) = 2π`, `cos 2π = 1`, `sin 2π = 0`); `geom_sum` is the telescoping `(a−1)·Σ_{k<N} aᵏ = aᴺ − 1` (pure field algebra in `C`). The capstone `dft_orthogonality`: `Σ_{k<N} ((w N)ʲ)ᵏ = N` if `(w N)ʲ = 1`, else `0` — the character-sum orthogonality of the DFT rows. Its vanishing branch rests **only** on `aᴺ = 1` (so `aᴺ − 1 = 0`) plus the geometric series, all in `C`; the trig/Reals input enters *solely* to establish `(w N)ᴺ = 1`. So the finite Fourier orthogonality that `WalshHadamard` proves over `F₂³` (`H² = 8I`, `|G| = 8`) now holds over `ℂ` for arbitrary `N` (`Σ = N` or `0`) — on our own axiom-disciplined `C`, no external complex library. Classical Reals axioms (quarantined, via `C`/trig). **Primitivity + the Kronecker delta:** `w_primitive` proves `(w N)ʲ ≠ 1` for `0 < j < N` — the one real-analysis input, from `cos(2πj/N) < 1` strictly on `0 < 2πj/N < 2π` (via `cos x = 1 − 2·sin²(x/2)` and `sin(x/2) > 0` on `(0,π)`, `cos_lt_1`). This upgrades the orthogonality to the unconditional **Kronecker-delta** form `dft_orthogonality_delta`: for `j < N`, `Σ_{k<N} (w N)^{jk} = N` if `j = 0`, else `0` — a genuine `N·[j ≡ 0]`, exactly the input a full ℂ-DFT inversion (`F⁻¹F = id`) needs.

`DFTInversion.v` is the **complex DFT inversion theorem** on the custom `C` — the `F⁻¹F = id` capstone the roots-of-unity orthogonality was built for. With `wc N := conj(w N)` (the inverse root, `wc·w = 1`), the length-`N` transform and inverse are `(DFT f) m = Σ_{k<N} f k · (wc N)^{mk}` and `(IDFT g) k = (1/N)·Σ_{m<N} g m · (w N)^{mk}`, and `dft_inversion` proves `IDFT (DFT f) k = f k` for `k < N` — the finite Fourier transform on `ℂ` is genuinely **invertible**, the ℂ analogue of `WalshHadamard`'s `H² = 8I` (which is `F⁻¹F = id` up to `1/8`). The engine is **two-index orthogonality** `orthogonality_2`: `Σ_{m<N} (wc N)^{ml}·(w N)^{mk} = N` if `k=l` else `0`, whose off-diagonal vanishing uses the **injectivity** of `j ↦ (w N)^j` on `{0,…,N−1}` (`w_pow_inj`, from `w_primitive`). The inversion is then pure `C`-field algebra: finite **Fubini** (`Csum_swap`), scalar factoring (`Csum_scale_l/r`), and **delta extraction** (`Csum_delta`), collapsing `(1/N)·Σ_m Σ_l f l·(wc)^{ml}(w)^{mk}` to `f k`. This closes the finite-Fourier arc: the orthogonality/inversion `WalshHadamard` established over `F₂³` (axiom-free) now holds over `ℂ` for arbitrary `N`, on the repo's own axiom-disciplined `C`. Classical Reals axioms (quarantined, via `C`/trig).

`DFTConvolution.v` proves the **convolution theorem** — the DFT **diagonalizes cyclic convolution**. For `(f⋆g)(n) = Σ_{k<N} f k · g((n−k) mod N)` (`Cconv`), `conv_theorem`: `DFT (f⋆g) m = DFT f m · DFT g m` — cyclic convolution becomes pointwise product in the transform domain. With `DFTInversion` (`F⁻¹F = id`) this is the full algebraic toolkit for the finite Fourier transform on `C`. The one genuinely new ingredient is that a **cyclic shift permutes** `{0,…,N−1}` (`rotation_perm`, via `NoDup_Permutation_bis` + injectivity of `n ↦ (n+c) mod N`), giving the reindexing lemma `Csum_reindex` (`Σ_n φ((n+c) mod N) = Σ_n φ n`, proved through the `Csum ↔ fold_right` bridge and permutation-invariance of the fold `fold_Cadd_perm`); combined with **root periodicity** `Cpow_wc_mod` (`(wc N)^a` depends only on `a mod N`) it yields the **shift theorem** `dft_shift` (`Σ_n g((n−k) mod N)·(wc N)^{mn} = (wc N)^{mk}·DFT g m`), and the convolution theorem then follows by finite **Fubini** (`Csum_swap`) + scalar factoring — all `C`-field algebra. Classical Reals axioms (quarantined, via `C`/trig).

`Parseval.v` proves **Parseval/Plancherel** — the DFT is (up to the factor `N`) an **isometry** for the Hermitian inner product. `plancherel`: `Σ_{k<N} f k·conj(g k) = (1/N)·Σ_{m<N} (DFT f m)·conj(DFT g m)`; `parseval_norm`: `Σ_{k<N} |f k|² = (1/N)·Σ_{m<N} |DFT f m|²` (with `|z|² = Cnorm2 z` via `Cmul_conj`). Conjugation commutes with the transform (`conj_wc_pow`: `conj((wc N)^j) = (w N)^j`, from `Cconj_pow` + `Cconj_involutive`), so `conj(DFT g)` is again an analysis sum; the key lemma `sum_wc_conjDFT` (`Σ_m (wc N)^{mk}·conj(DFT g m) = N·conj(g k)`) is the same `orthogonality_2` + Fubini + `Csum_delta` pattern as inversion, and Plancherel follows by one more Fubini and the `1/N` cancellation (`field`, `RtoC(INR N) ≠ 0`). This **completes the finite Fourier analysis on `C`**: inversion (`F⁻¹F = id`), the convolution theorem (diagonalization), and now Parseval (isometry) — the three theorems that make the DFT a complete analytic tool — all on the repo's own axiom-disciplined `C`, adding nothing beyond the classical `ℝ` axioms. Classical Reals axioms (quarantined, via `C`/trig).

`CharactersModN.v` reads the DFT orthogonality as **character theory**: the characters of the cyclic group `(ℤ/Nℤ, +)` are `χ_a(n) = (w N)^{an} = e^{2πi·an/N}`, and they form the dual group `ℤ/Nℤ`. Proved: `chi_add` (`χ_a(m+n) = χ_a(m)·χ_a(n)` — a genuine character), `chi_0` (`χ_a(0)=1`), `chi_pow_N` (values are `N`-th roots of unity), `chi_mul` (`χ_a·χ_b = χ_{a+b}` — the dual group), `chi_periodic_n` (well-defined on `ℤ/Nℤ`), and the **two orthogonality relations** of the character table: `char_orthogonality_row` (`Σ_{n<N} χ_a(n)·conj(χ_b(n)) = N·[a=b]`) and `char_orthogonality_col` (`Σ_{a<N} χ_a(n)·conj(χ_a(m)) = N·[n=m]`) — the rows and columns are orthogonal with squared norm `N`, exactly `orthogonality_2` in character language (via `conj_w_pow`: conjugation flips `w`↔`wc`). **Honest scope:** these are the characters of the *cyclic* group `ℤ/Nℤ` — the finite-abelian-group orthogonality at the heart of Dirichlet character theory. A Dirichlet character mod `N` is a character of the *multiplicative* group `(ℤ/Nℤ)*`; when that group is cyclic (e.g. `N` prime) its characters are exactly these via a primitive root, but the group `(ℤ/Nℤ)*` itself, primitivity, and Dirichlet L-series / Euler products need the multiplicative structure and are **not** built here. Classical Reals axioms (quarantined, via `C`/trig).

`ZmodPStar.v` is **Phase 1 of the actual-Dirichlet-mod-p build** (closing the gap above): the multiplicative group `(ℤ/pℤ)*` of units mod a prime and **Fermat's little theorem** `fermat`: `1 ≤ a ≤ p−1 ⟹ a^{p−1} mod p = 1` — **axiom-free** ("Closed under the global context"). Units are the residues `[1,p−1]`; we prove closure of `(·) mod p` (`mulmod_in_units`, via Euclid's lemma bridged `nat`↔`ℤ` as `prime_mult_nat`), the mod-`p` **cancellation law** `cancel_mod` (via `Gauss`, reusing `ProfiniteCRT`'s congruence↔divisibility lemmas), and Fermat by the classical product argument: `x ↦ (a·x) mod p` **permutes** the units (`units_perm`, `NoDup_Permutation_bis` + injectivity — the same permutation technique as `DFTConvolution`), so `a^{p−1}·(p−1)! ≡ (p−1)! (mod p)` and `(p−1)!` cancels (it's coprime to `p`, `Pi_coprime`). This is the group-theoretic foundation for primitive roots and genuine Dirichlet characters mod `p`; **axiom-free** (constructive `ℤ`/`nat` + `Znumtheory`).

`Totient.v` is **Phase 2**: Euler's totient `φ` and the **divisor-sum identity** `totient_divisor_sum`: `Σ_{d∣n} φ(d) = n` (for `n ≥ 1`) — **axiom-free** (pure `nat` + `gcd`). Proof by partitioning `[1,n]` via the key `e(k) := n/gcd(k,n)` (always a divisor, `key_in_divisors`): the fiber over `e` has size `φ(e)` (`count_key_eq_phi`, via a *membership*-`Permutation` — no order reasoning — between the fiber and `{c·j : j∈[1,e], gcd(j,e)=1}` using `Nat.gcd_mul_mono_l`), and the fibers partition `[1,n]` (`disjoint_filter_sum`: the sum of disjoint-key filter-counts equals the count of the union of keys, which is all of `[1,n]`). This is the counting input for the order-counting primitive-root proof (Phase 4).

`PolyRootsFp.v` is **Phase 3** — the field-theoretic heart: a degree-`d` polynomial over `𝔽_p` has at most `d` roots, hence `dth_roots_bound`: `#{a ∈ [1,p−1] : a^d mod p = 1} ≤ d` — **axiom-free**. A minimal polynomial theory over `ℤ` (little-endian coefficient lists, Horner `peval`), **synthetic division** `sdiv` (Ruffini's rule, exact over `ℤ`: `peval P x = peval P r + (x−r)·peval (sdiv P r) x`), the **factor theorem mod p** (`factor_mod`), and the **Lagrange bound** `roots_le` by strong induction on the degree: at a root `r`, factor out `(X−r)`, and every other root is a root of the degree-`(d−1)` quotient (using that `𝔽_p` is an **integral domain** via `prime_mult`, so `(x−r)·Q(x) ≡ 0` forces `x=r` or `Q(x)≡0`), bounding the root count via `NoDup_incl_length`; the invariant is a nonzero leading coefficient (`sdiv_lead`: division preserves it). `dth_roots_bound` instantiates `roots_le` at `X^d − 1` (`peval_monomial`, `xdm1`, `last = 1`), bridging the `nat` count to the `ℤ` polynomial root count (`length_filter_map`, `mod_pred_nat`: `(X−1) mod p = 0 ⟺ X mod p = 1`). This is the input for the order-counting primitive-root proof (Phase 4).

`ZmodOrder.v` is **Phase 4a** — the multiplicative **order** `ord p a` of a unit mod a prime (least `k ≥ 1` with `a^k ≡ 1`, found by searching `[1,p−1]`), and its theory — **axiom-free**. With `pw p a k := a^k mod p`: `ord_period` (`pw p a (ord p a) = 1`), `ord_least` (leastness), `ord_divides` (`pw p a k = 1 ⟹ ord p a ∣ k`, via Euclidean division `k = ord·(k/ord) + k mod ord` and `pw_pow_ord_mul`), `ord_div_pm1` (`ord p a ∣ (p−1)`, via Fermat), and `pow_inj_below` (the powers `a^0,…,a^{ord−1}` are pairwise distinct mod p — from leastness + the cancellation `pw_cancel` reusing `ZmodPStar.cancel_mod`). Supporting pow-mod arithmetic (`pw_add`, `pw_mul_exp`, `pow_mod`, `not_div_pow`) and a `firstsat`/`firstsat_min` least-element search. This is the foundation for the order-counting primitive-root proof; the counting/squeeze (`ψ(d) ≤ φ(d)`, `Σψ = Σφ = p−1 ⟹ ∃` primitive root) is **Phase 4b** (`PrimitiveRoot.v`).

`PrimitiveRoot.v` is **Phase 4b** — the payoff: **`(ℤ/pℤ)* is cyclic`**, `units_cyclic`: for prime `p` there exists `g ∈ [1,p−1]` with `ord p g = p−1` — a **primitive root** — proven from scratch, unconditionally, **axiom-free**. The order-counting argument, with `ψ(d) := #{units of order d}`: **`psi_le_phi`** (`ψ(d) ≤ φ(d)`) — if `a₀` has order `d`, every order-`d` unit is `a₀^i` with `gcd(i,d)=1`, the crux being **surjectivity onto roots** (`root_is_power`: every solution of `x^d=1` is a power of `a₀`, since the `d` distinct powers `pow_inj_below` form a NoDup length-`d` sublist of the `≤ d` roots `dth_roots_bound`, so `NoDup_length_incl` makes them exhaust the roots) plus **`gcd_of_order`** (an order-`d` power has coprime index) and **`coprimes_below_eq_phi`** (`#{i<d : gcd(i,d)=1} = φ(d)`); **`sum_psi`** (`Σ_{d∣p−1} ψ(d) = p−1`, partitioning units by order); and the **squeeze** — `Σψ = Σφ = p−1` (`Totient.totient_divisor_sum`) with `ψ ≤ φ` termwise forces `ψ = φ` (`sum_eq_termwise`), so `ψ(p−1) = φ(p−1) ≥ 1` (`phi_pos`). This closes the from-scratch cyclicity theorem — every ingredient (`ZmodPStar`, `Totient`, `PolyRootsFp`, `ZmodOrder`, this) axiom-free. Feeds the Dirichlet-character construction (Phase 5).

`DirichletModP.v` is **Phase 5, the finish line**: **genuine Dirichlet characters mod a prime `p` and their orthogonality**, transported from the cyclic-group characters of `ℤ/(p−1)ℤ` (`CharactersModN`) through the **discrete logarithm** base a primitive root (`PrimitiveRoot.units_cyclic`). With `dlog p g m` (the `k` with `gᵏ ≡ m`, `dlog_pow`/`dlog_inv` via `pow_inj_below` + `powers_units_perm` — every unit is a power of `g`) and `dchar p g a n := if p∣n then 0 else (w(p−1))^{a·dlog(n mod p)}`: `dchar_1` (`χ_a(1)=1`), `dchar_zero` (vanishes on multiples of `p`), `dchar_periodic` (period `p`), and `dchar_on_power` (`χ_a(gᵏ) = χ_a^{ℤ/(p−1)ℤ}(k)`, the transport). The capstone **`dirichlet_orthogonality`**: `Σ_{n=1}^{p−1} χ_a(n)·conj(χ_b(n)) = (p−1)·[a=b]` — proved by reindexing the sum over units to a sum over exponents (`n = gᵏ`, `powers_units_perm` + `fold_Cadd_perm`) and applying `CharactersModN.char_orthogonality_row` with `N = p−1`. `dirichlet_characters_mod_p` bundles it: for prime `p` there is a primitive root `g` giving characters with all these properties. **This ties the axiom-free cyclicity theorem to the custom-`C` roots-of-unity / DFT-orthogonality machinery — the whole 5-phase from-scratch Dirichlet-mod-p construction, unconditional.** Classical Reals axioms (quarantined, via `C`/trig; the number-theoretic core Phases 1–4b is entirely axiom-free).

`DirichletLEuler.v` extends the Dirichlet characters into the **finite Dirichlet L-function and its Euler product**. The reason an L-series factors over primes is that a Dirichlet character is **completely multiplicative** — and that bullet, only *advertised* by `DirichletModP`, is now *proved* here from scratch: `dchar_mul` (`χ(mn) = χ(m)χ(n)`, including the `p∣m`/`p∣n` vanishing cases) via the **discrete-log homomorphism** `dlog_mul` (`dlog(uv) ≡ dlog u + dlog v (mod p−1)`, itself from `pw_mod` — the power map's period-`(p−1)` periodicity — plus `pow_inj_below`), and `dchar_pow` (`χ(qᵏ) = χ(q)ᵏ`). Consequences over the custom field `C`: the **local Euler factor** `Llocal q x K = Σ_{k<K} χ(qᵏ) xᵏ = Σ_{k<K} (χ(q)x)ᵏ` (`Llocal_geom`, by `dchar_pow`) with the geometric closed form `local_euler_factor`: `(1 − χ(q)x)·Llocal = 1 − (χ(q)x)^K` (→ `1/(1−χ(q)x)`, from `RootsOfUnity.geom_sum`); and the **finite Euler product** — the primon-gas state-sum machinery (`PrimonGas`) ported to `C` (`euler_product_C`: `Σ_states ∏ xᵢ^{kᵢ} = ∏ᵢ Σ_{k<K} xᵢᵏ`) and instantiated at the **character-twisted fugacities** `χ(q)·x_q`, giving `dirichlet_L_euler_product`: `Σ_states ∏_q (χ(q)x_q)^{k_q} = ∏_q Llocal_q` — the state-sum equals the product of local Euler factors. `dirichlet_L_function` bundles all four. Read with `x_q = q⁻ˢ` and `K → ∞`, `∏_q Llocal_q = L(s,χ) = Σ_n χ(n) n⁻ˢ`: the Dirichlet L Euler product; the infinite product and the state↔integer (unique-factorisation) bijection stay prose, the finite identities are proved. Classical Reals axioms (quarantined, via `C`/trig).

`GaussianIntegers.v` **bridges the complex construction into `ℤ`**: it runs `ComplexField`'s parametric ring core over `ℤ` instead of `ℝ`, giving the ring of integers `ZI = ℤ[e]/(e²=δ) = ℤ×ℤ` — and over `ℤ` the whole thing is **axiom-free** ("Closed under the global context"), a strictly cleaner footprint than the `ℝ` version. The **weak ring core** `ZIring_theory_g` proves `ℤ[e]/(e²=δ)` is a commutative ring for *every* integer `δ` (`ZImul` is the `δ=−1` Gaussian instance, `ZIi_sq` an `e²=δ` downstream fact). **The bridge is the multiplicative norm `N_δ : ZI → ℤ`**, `N_δ(a) = Re²−δ·Im²`: `ZInorm_mul` proves `N_δ(a·b) = N_δ(a)·N_δ(b)` (a genuine ring→`ℤ` homomorphism, polynomial identity for all `δ`) and `ZImulg_conj` proves `a·conj a = N_δ(a)` landed in `ℤ`. `ℤ ↪ ZI` is an injective ring hom (`ZtoZI_add`/`ZtoZI_mul`/`ZtoZI_inj`), conjugation is an involution/hom. For the Gaussian case `δ=−1` the norm is `Re²+Im² ≥ 0` (`ZInorm_nonneg`), so `a·b=1 ⟹ N(a)=1` (`unit_norm`, product of nonnegatives) and the **units are exactly `{1,−1,i,−i}`** = the norm-1 elements (`gaussian_units`), each its own inverse via its conjugate (`norm1_unit`). `gaussian_integers` bundles it all. This is the arithmetic shadow of `ComplexField` and the entry point for sums-of-two-squares / Gaussian-prime number theory; `δ<0/=0/>0` = Gaussian / dual / split integers. **Axiom-free.**

`SumTwoSquares.v` uses the Gaussian-norm bridge for **sums of two squares**. `sum2 n := ∃ a b, n = a²+b²`, and `sum2_norm` identifies it with being a *norm from `ℤ[i]`* (`n = ZInorm(−1) z`). **Pillar 1 — Brahmagupta–Fibonacci** (`sum2_mul`): sums of two squares are closed under multiplication, `(a²+b²)(c²+d²) = (ac−bd)²+(ad+bc)²` — proved as *exactly* the multiplicativity of the Gaussian norm `N(x·y)=N(x)·N(y)` (`GaussianIntegers.ZInorm_mul`), the bridge in action; `brahmagupta_fibonacci` records the explicit identity. **Pillar 2 — `−1` is a quadratic residue mod `p` when `p ≡ 1 (mod 4)`** (`neg1_QR`): there is an `x` with `x²+1 ≡ 0 (mod p)`, proved from the repo's **from-scratch cyclicity** of `(ℤ/pℤ)*` (`PrimitiveRoot.units_cyclic`) — take `x = g^{(p−1)/4}` for a primitive root `g`; then `x² = g^{(p−1)/2}` is a square root of `1` (`ord_period`) that is not `1` (`pow_inj_below`, since `0 < (p−1)/2 < ord`), hence `≡ −1` via the sqrt-of-1 fact `sqrt1` (`y²≡1 ⟹ y∈{1,p−1}`, Euclid's lemma `prime_mult_nat` on `(y−1)(y+1)`). `sum_two_squares_pillars` bundles both. These are the two ingredients of Fermat's `p ≡ 1 (mod 4) ⟹ p = a²+b²`: pillar 2 gives `p ∣ x²+1 = N(x+i)`, and a descent (Thue/Euler, using pillar 1 as its engine) forces `p` itself to be a norm — the descent is the next target. **Axiom-free** ("Closed under the global context").

`EulerReindex.v` performs the **reindexing along the `code` bijection** — transporting the primon-gas Euler product onto the number line as a partial ζ-sum. `PrimonGas.euler_product` already gives the distributive law `Σ_{occupation states} weight xs ks = ∏_modes psum xᵢ K` axiom-free; the new content connects `weight` to `code`. With fugacity `fug p = 1/(inject_Z p)²`, the key identity `weight_fug` proves the primon-gas Boltzmann weight of an occupation vector **is** the reciprocal square of its coded number: `weight (map fug ps) ks = 1/(code ps ks)²` (via `qpow`/`inject_Z` homomorphism lemmas `inj_pow`, `qpow_mul`, `qpow_inv`, `qpow_mult_exp`, and a `qpow` setoid morphism — the ℚ inverse identities are unconditional). Hence `euler_reindex`: `∏ᵢ (Σ_{k<K} pᵢ⁻²ᵏ) = Σ_{occupation states ks} 1/(code ps ks)²` — the finite Euler product **reindexed as a sum of `1/m²` over the smooth numbers `m = code ps ks`** — and `codes_distinct` (from `code_inj`) shows those coded numbers are pairwise distinct, so the RHS is a sum over *distinct* `{ps}`-smooth numbers. This is exactly the transport that turns the primorial Euler tower into a partial ζ-sum. **Axiom-free** ("Closed under the global context").

`EulerProductZeta.v` is the **capstone: the Euler product formula for ζ(2)** — `∏_{p prime, p ≤ B} (1−p⁻²)⁻¹ → ζ(2)` as `B → ∞` (`euler_product_zeta2`, an honest `Un_cv`). This closes the whole arc *primon gas → reindex → factorization bijection → squeeze*, unconditionally. It is a squeeze between the two bridge halves: the **upper** bound is `EulerProductZetaBound.euler_factor_le_zeta` (`∏_{p≤B} ≤ ζ(2)`), and the **lower** bound `zpart_le_euler` (`Hlower`, proved here) is `zpart N ≤ ∏_{p≤N+1}(1−p⁻²)⁻¹` — because every `m ∈ {1..N+1}` is `{primes ≤ N+1}`-smooth (`small_smooth`, via `Z.divide_pos_le`), so by factorization **existence** (`PrimeFactorizationExists.code_surj`) with bounded exponents (`entry_pow_le_code` + `nat_lt_pow2` ⟹ each exponent `< S N`, so the tuple is in `gstates` via `gstates_complete`), its `1/m²` appears among the coded terms; `incl_sum_le` then dominates `zpart N` by the reindexed partial product, and `Zpartial_le_Zfactor` lifts to the full factor. Since the upper bound is the *constant* `ζ(2)` and `zpart N → ζ(2)`, `squeeze_const_upper` finishes. Supporting infrastructure: a prime enumeration `primes_upto B` (all primes `≤ B`, `NoDup`, complete — via `filter`/`prime_dec`). **Honest scope:** this is the genuine classical Euler product identity *at `s = 2`* — existence of the product limit and its equality to `ζ(2)` (itself an existence limit, *not* the value `π²/6`); general `s`, `−ζ'/ζ`, and anything about zeta *zeros* remain out of scope. Classical Reals axioms (quarantined). This is the first result in the repo that reaches an actual theorem of classical analytic number theory rather than a finite/structural fragment.

`PrimorialZeta.v` attempts the capstone — **the primorial tower's limit is ζ(2)**, i.e. the actual Euler product formula `∏_p (1−p⁻²)⁻¹ = Σ_n 1/n²`. This is a genuine, hard theorem, so we scope it as an **honest reduction**: the number-theoretic crux (Euler's factorization `EP n = Σ_{{first n primes}-smooth m} 1/m²`, which needs unique factorization over the first `n` primes + a sum reindexing) is **isolated as exactly its two consequences**, taken as hypotheses — `Hupper : ∀n, EP n ≤ Lz` (smooth ⊆ all ℕ) and `Hlower : ∀N, ∃n, zpart N ≤ EP n` (every initial ζ-segment is eventually dominated by a rung, since small numbers are eventually smooth). Given these, `tower_is_zeta` proves the **analytic squeeze in full**: `EP_converges` gives `lim EP = L`; `growing_ineq` gives `EP n ≤ L`; the reusable `lim_le` (a convergent sequence bounded above by `c` has limit `≤ c`, proved from `Rabs_def2`) gives `L ≤ Lz` and `Lz ≤ L`; `Rle_antisym` closes `L = Lz`, so `Un_cv (EP P) Lz`. The corollary `tower_is_zeta2` instantiates `Lz` at the genuine `ζ(2)` limit from `ZetaConverge.zeta2_converges`. So the two infinite processes we built — the sum `Σ 1/n²` and the primorial product tower — are proven to have the **same limit**, *modulo* the cleanly-named factorization crux (the deferred piece: unique factorization over the first `n` primes + reindexing + "small numbers are eventually smooth"). Classical Reals axioms (quarantined).

`AbelSummation.v` opens the **contour-free prime bridge**. The analytic theory reaches primes through a *contour integral* (Perron + the residue theorem, whose invariant is the winding number) — machinery our system deliberately lacks (no ℂ, no Cauchy's theorem, no analytic continuation). So we use its elementary shadow: **summation by parts**, whose "invariant" is the telescoping boundary term. `abel_summation` proves `Σ_{k=0}^{S M} aₖbₖ = A_{S M}·b_{S M} − Σ_{k=0}^{M} Aₖ(b_{k+1}−bₖ)` (with `Aₙ = Σ_{j≤n} aⱼ`), by induction + `ring` on the peeled top terms. This is the discrete integration-by-parts that stands in for the contour shift; together with the convolution identity `Λ = μ⋆log` (`VonMangoldt`) it is the pre-Riemann toolkit behind Chebyshev's `ψ(x) ≍ x`. **Honest scope:** this is a foundational lemma, not yet the prime-counting theorem — the elementary route (contour-free) caps at Chebyshev order-of-magnitude bounds; the sharp `ψ(x) ∼ x` (PNT), the explicit formula, and anything about zeros genuinely require the contour step / ℂ we don't build. Classical Reals axioms (quarantined).

`VonMangoldtGlobal.v` proves the **global von Mangoldt identity** `Σ_{d|n} Λ(d) = log n` (`vonmangoldt_identity`) — the arithmetic crux of the contour-free prime bridge (the input to Chebyshev's `ψ(x) ≍ x` alongside `AbelSummation`), globalising `VonMangoldt` (per-prime) to a genuine `Λ : nat → R` over all `n`. Everything stays in `nat` (`Nat.gauss`) + `R` (for `ln`), no ℂ. The build, in order: a smallest-prime-factor `spf` **proved prime** (`spf_nprime`); the divisor sum `dsum` with permutation-invariance; the **coprime divisor split** `c = gcd(c,a)·gcd(c,b)` (`split_divisor`, the delicate Gauss keystone — each direction a `Nat.gauss` argument, `Nat.gcd_div_gcd` for the divide-out coprimality); the **divisor-list bijection** `divisors(a·b) ≅ divisors(a)×divisors(b)` (`divisors_prod_perm`) and the `dsum` reindexing `dsum_prod`; the global `Λ` (`Λ n = ln(spf n)` iff `is_pow (spf n) n`, so `Λ 1 = 0` free) with `Lam_mul_zero` (`Λ(d·e)=0` for coprime `d,e≥2`, via `is_pow_true_pow` + `prime_dvd_prime_pow`); the **multiplicativity** `dsum Λ (a·b) = dsum Λ a + dsum Λ b` (`dsum_mult`, pure sum manipulation — `Rsum_plus`/`Fubini_list_prod`/`Rsum_if1`); the **prime-power base** `dsum Λ (p^k) = ln(p^k)` (`dsum_primepow`, divisors of `p^k` are its powers); and the **assembly** — strong induction peeling `p=spf n`, `n=p^a·m` coprime (`pval`), via `dsum_mult` + base + IH + `ln_mult`. **Fully axiom-free in `nat`; the `R` layer uses only the quarantined classical Reals axioms** (`ln`). This closes the general-`n` divisor-sum gap the LEDGER had long flagged.

`Chebyshev.v` performs the **order-swap** at the heart of the contour-free prime bridge: `Σ_{n≤N} log n = Σ_{d≤N} Λ(d)·⌊N/d⌋` (`order_swap_identity`) — the Dirichlet-hyperbola form, i.e. the elementary analogue of Perron's formula. It re-sums `VonMangoldtGlobal`'s `Σ_{d|n} Λ(d) = log n` by counting, for each `d`, how many `n ≤ N` it divides (`= ⌊N/d⌋`). The proof needs **no Fubini**: both `Σ_{n≤N} log n` (= `Σ_{n≤N} Σ_{d|n} Λ(d)`, `Tlog_eq_sumdsum`) and `Σ_{d≤N} Λ(d)⌊N/d⌋` satisfy the *same* recursion `f(S N) = f N + dsum Λ (S N)` (`sumdsum_rec`, `chsum_rec`), so a one-line induction (`order_swap`) closes it — the key nat fact being `⌊(N+1)/d⌋ = ⌊N/d⌋ + [d ∣ N+1]` (`div_succ_indicator`, via `div_mod` + `nia`) and the masked-sum form of `dsum` (`dsum_masked`). The file also lands the **factorial–log bridge** toward the central binomial: `Tlog_eq_ln_fact` proves `T(N) = Σ_{n≤N} log n = log(N!)` (via `Tlog_rec` + `ln_mult`/`mult_INR`/`fact_simpl`), so `D(N) = T(N) − 2T(⌊N/2⌋) = log(N! / (⌊N/2⌋!)²) = log(central binomial)` — reducing `ψ ≍ x` to the elementary bounds `4^M/(2M+1) ≤ C(2M,M) ≤ 4^M` (row sum + unimodality), the remaining real-analysis layer. Classical Reals axioms (quarantined). The file also lands the **combinatorial building blocks** of the final squeeze: `Lam_nonneg` (`Λ ≥ 0`, via `spf n ≥ 1` and `ln ≥ 0` on `[1,∞)`), the floor lemma `floor_half_step` (`⌊N/d⌋ − 2⌊N/(2d)⌋ = (⌊N/d⌋) mod 2 ∈ {0,1}`, via `Nat.div_div`), and the prime-counting `ψ`. **Remaining for `ψ(x) ≍ x`:** the combinatorial sandwich `ψ(N) − ψ(⌊N/2⌋) ≤ D(N) ≤ ψ(N)` for `D(N) = T(N) − 2T(⌊N/2⌋) = Σ_d Λ(d)·((⌊N/d⌋) mod 2)` (order-swap + `Lam_nonneg` + the floor lemma) reduces it to the single **numerical input** `D(N) ≈ N·log 2` — an elementary-Stirling / central-binomial estimate, a real-analysis layer left for a dedicated pass. That is the last elementary step before the contour-free ceiling (PNT/zeros stay out of reach).

`INFMonoid.v` identifies the repo's **3-symbol algebra `{I, N, F}`** (Cayley table: `I` identity, `N` involution `N∘N=I`, `F` absorbing zero) as the **Möbius / sign value monoid**, positioning it exactly at the junction of the two arcs. It proves (axiom-free): `{I,N,F}` is a **commutative monoid**; the sign embedding `val` (`I↦+1, N↦−1, F↦0`) is an **injective monoid homomorphism** (`val_hom`: the table *is* integer multiplication of signs; `val_inj`), so `({I,N,F},∘) ≅ ({+1,−1,0},×) = (𝔽₃,×)` — with unit group `{I,N} ≅ Z/2`, the **Walsh sign group** (`val` on units mirrors `WalshHadamard.signb_xorb`), and `F` the unique non-unit; and `sym_of_mu`: `val(sym_of k) = mu_pp k`, i.e. `{I,N,F}` is **exactly the value-monoid of the Möbius function on prime powers** (`MobiusReciprocal.mu_pp`: `I=μ(p⁰)`, `N=μ(p¹)`, `F=μ(p^{≥2})`), `F`'s absorption being the squarefree collapse. So the 3-symbol algebra is the alphabet of the reciprocal Euler factor `(1−x)=Σ_k μ(pᵏ)xᵏ` and of all Möbius inversion downstream: its **unit group drives the Fourier/sampling arc** (`WalshHadamard`/`WalshSampling`), its **full monoid drives the Euler-product/Möbius arc** (`MobiusReciprocal`→`EulerProductZeta`, `VonMangoldtGlobal`), and the shared involution `N²=I` is the reflection both turn on. "Closed under the global context."

`INFProduct.v` inducts the 3-symbol algebra up to **many observers** — the `n`-fold product monoid `({I,N,F})ⁿ` over a *list* of observers, built by induction (append one observer at a time). Each observer carries a `{I,N,F}` state; two configurations "observe each other" by the componentwise product `op_n` (a commutative monoid, identity `repeat I n`, `op_n_comm`/`op_n_assoc`/`op_n_id_l`). The configuration's sign is the **product of the per-observer signs** `val_n = ∏ val`, and its properties make the "observer/observed ratio is an arbitrary choice" precise:
- `val_n_op`: observers combine multiplicatively (`val_n (op_n a b) = val_n a · val_n b`) — the `n`-fold homomorphism;
- **`val_n_app`: `val_n (a ++ b) = val_n a · val_n b`** — the total sign factors over *any* partition of the observer list, so splitting into "observers" vs "observed" is a free choice (this is exactly multiplicativity / coprime-split freedom, no length condition);
- **`val_n_zero_iff`: `val_n l = 0 ⟺ In F l`** — a *single* `F`-observer vetoes the whole configuration to `0` (the absorbing zero one dimension up = the squarefree obstruction / the node);
- `val_n_mu`: `n` prime-observers with exponents `ks` give `∏ᵢ μ(pᵢ^{kᵢ})` — the Möbius value of their product (multiplicativity, via `INFMonoid.sym_of_mu`).

So the `{I,N,F}` algebra scales to arbitrarily many observers by induction, with the observer/observed split free (`val_n_app`) and the one-`F`-veto collapse (`val_n_zero_iff`); its unit group `(Z/2)ⁿ` is the `F₂ⁿ` Walsh-character world (sampling), and its full monoid is the Möbius world over `n` primes. "Closed under the global context."

`ObserverTriad.v` defines a **Hermitian (self-adjoint) operator on the 3-way split `{Observer, Observed, Observation}`**. Over ℝ "Hermitian" is self-adjointness w.r.t. the real inner product (a symmetric matrix — the same notion `WalshHadamardHilbert` uses; no ℂ here). The operator `M` encodes **measurement**: the observation couples the observer and the observed (matrix `[[0,0,1],[0,0,1],[1,1,0]]`), and `M_self_adjoint` proves `⟨Mf,g⟩ = ⟨f,Mg⟩`. Its real spectrum is `{0, +√2, −√2}` with an **orthogonal eigenbasis** (`eig_0`/`eig_p`/`eig_m`, `ortho_*`): the eigenvalue-`0` mode is `v0 = Observer − Observed` — the difference the measurement *cannot resolve* (the kernel/gauge mode) — and `v± = Observer + Observed ± √2·Observation` are the two observable modes. This is the Hermitian spectral theorem's payoff on the triad (real eigenvalues, orthogonal eigenbasis) and connects to `Involution`'s residue decomposition (symmetric/antisymmetric split) and the `{I,N,F}` involution `N`. Classical Reals axioms (quarantined). **Honest scope:** a genuine finite self-adjoint operator with real spectrum; the observer/observed/observation labels are an *interpretation* — the theorems are about the symmetric operator `M`.

`ObserverSwap.v` is the **involution cousin** of `ObserverTriad`'s measurement operator: the observer↔observed **swap `S`** (matrix `[[0,1,0],[1,0,0],[0,0,1]]`, fixing the observation). It is proved **self-adjoint** (`S_self_adjoint`) *and* an **involution** (`S_involution`, `S∘S=I`), so a reflection with spectrum `{+1,−1}`. This realises the 3-symbol algebra's **unit group `{I,N} ≅ Z/2`** (`INFMonoid`) as a reflection on the triad: the antisymmetric mode `v0 = Observer − Observed` is **negated** (`S_eig_v0`, eigenvalue `−1`), and the symmetric modes `vs = Observer + Observed`, `von = Observation` are **fixed** (eigenvalue `+1`); orthogonal eigenbasis (`S_ortho_*`). The tie to the monoid is a theorem: `v0_is_N_mode`/`vs_is_I_mode` show the eigenvalues are literally `IZR(val N) = −1` and `IZR(val I) = +1` — the swap's `±1` spectrum *is* `{I,N}`, with `N`'s involution `N²=I` mirrored by `S²=I`. And `residue_mode` ties it to `ObserverTriad`: `v0` is **simultaneously** the measurement kernel (`M v0 = 0`, invisible to observation) and the swap's `−1` eigenvector (flipped by the observer/observed exchange) — the residue the measurement can't resolve is exactly what the swap negates. Includes the reflection **residue decomposition** `f = sym_part f + anti_part f` (`decomposition`, `sym_fixed`, `anti_negated`), the triad analogue of `Involution.v`. Classical Reals axioms (quarantined). Honest scope as `ObserverTriad`: role names are interpretation, the theorems are about `S`.

`ObserverSpectral.v` extends the observer arc to **commuting observables**: the measurement `M` (`ObserverTriad`) and the observer↔observed swap `S` (`ObserverSwap`) **commute** (`MS_commute`: `M∘S = S∘M`, `[M,S]=0`) and are **simultaneously diagonalized** by the single eigenbasis `{v0, vp, vm}` — `ObserverTriad`'s `M`-eigenvectors are *also* `S`-eigenvectors (`S_eig_vp`/`S_eig_vm`: `vp, vm` are `S`-fixed at `+1`; `v0` is `S`'s `−1` mode). So on the triad: `v0 = Observer − Observed` has `(M,S)`-eigenvalues `(0, −1)` — the measurement kernel and swap-negated; `vp/vm = Observer + Observed ± √2·Observation` have `(±√2, +1)` — the observable, swap-fixed modes. `commuting_observables` bundles `[M,S]=0` with the joint diagonalization: two self-adjoint observables (a measurement and a symmetry) that commute and share an orthogonal eigenbasis — the finite spectral picture of *simultaneously measurable* observables. Classical Reals axioms (quarantined); role names are interpretation, the theorems are about `M`, `S`.

`RecipSquareBound.v` is the **analytic crux of the Euler-product bridge**: any finite set of *distinct positive integers* contributes at most `ζ(2)` worth of `1/m²`. `recip_sq_nodup_bound`: `NoDup L → (all m∈L ≥ 1) → Σ_{m∈L} 1/m² ≤ ζ(2)`. This is exactly what forces the primorial rung `EP n ≤ ζ(2)` — *tighter* than the telescoping `EP n ≤ 2`, and it must be, since `ζ(2) < 2`: via `EulerReindex`, `EP n` is a sum of `1/m²` over the **distinct** `{first n primes}`-smooth numbers, a subset of all `{1/m²}`, hence `≤ ζ(2)`. Proof chain: a `NoDup` list of positive integers included in `[1..M]` sums (of any nonnegative weight) to at most the full-range sum (`incl_sum_le`, by peeling elements with `List.remove` — `sum_remove`, `remove_nodup`); the full-range sum of `1/m²` over `[1..M]` is the ζ-partial-sum `zpart (M-1)` (`seqsum_zpart`); and every ζ-partial-sum is `≤` the limit (`growing_ineq`, from `ZetaConverge`). Classical Reals axioms (quarantined). This is part 1 of the analytic bridge — the ℝ-side domination; part 2 (`EulerProductZetaBound.v`) is the ℚ→ℝ transport.

`EulerProductZetaBound.v` **completes the analytic bridge** — for any list `ps` of *distinct primes*, the finite Euler product `∏_{p∈ps} (1−p⁻²)⁻¹ ≤ ζ(2)`. This is `Hupper` (the upper half of `PrimorialZeta`) for the concrete prime enumeration, and it is *tighter* than the telescoping `EP ≤ 2` (necessary, since `ζ(2) < 2`). It transports `EulerReindex.euler_reindex` (a ℚ identity) through `Q2R` into `RecipSquareBound`: `euler_partial_reindex_R` shows the ℝ partial product `Zpartial (map (p↦1/p²) ps) N` **equals** the sum of `1/m²` over the coded numbers `m = code ps ks` (via `Q2R` homomorphism lemmas `Q2R_qprod`/`Q2R_qsum`/`Q2R_qpow`/`Q2R_recip_qpow2` and the index-aligning `Rlsum_seq_sumf`); `codes_nat_nodup` (from `code_inj` + `gstates_nodup`, itself from a `NoDup_app`-based `nodup_flatmap_cons`) and `code_pos` show those are **distinct positive integers**, so `recip_sq_nodup_bound` gives `Zpartial … N ≤ ζ(2)`; and `euler_factor_le_zeta` takes `N→∞` (`euler_product_R` + `lim_le`). So the full chain **primon gas → reindex → factorization bijection → ζ(2) domination** is now machine-checked: the primorial Euler rung is provably `≤ ζ(2)`. Classical Reals axioms (quarantined).

`ZetaConverge.v` adds a genuine **infinite process**: the Dirichlet series `ζ(2) = Σ_{n≥1} 1/n²` **converges** — the first genuinely‑infinite, over‑*all*‑numbers object in the arc (the total primon‑gas partition function at `s=2`, not a per‑prime factor or a finite product). Proof by **monotone‑bounded convergence** (`growing_cv`): the partial sums `zpart` are increasing (`zpart_growing`) and bounded above by `2` (`zpart_ub`) via the telescoping estimate `1/(m+1)² ≤ 1/m − 1/(m+1)` (`zpart_bound`). `zeta2_converges : { l | Un_cv zpart l }`. **Honest scope:** proves the limit *exists*, not its value `π²/6`; the same bound gives convergence of `Σ 1/nˢ` for every real `s ≥ 2`. The infinite Euler product `ζ = ∏_p(1−p⁻ˢ)⁻¹` over all primes and the `1<s<2` range stay out of scope. Uses the classical Reals axioms (quarantined).

`EulerProductR.v` fills the **per‑prime → finite product** gap on the analytic side: for any finite list of fugacities `xs` (each `|x| < 1`), the product of the truncated geometric partial sums converges to the product of Euler factors — `euler_product_R : Un_cv (Zpartial xs) (Zfactor xs)`, i.e. `∏_{x∈xs} Σ_{k≤N} xᵏ → ∏_{x∈xs} 1/(1−x)`, the **finite Euler product** `∏_p (1−p⁻ˢ)⁻¹` as `N→∞`. Proof: each factor converges (`EulerFactorR.geom_limit`) and a finite product of convergent sequences converges to the product of limits (stdlib `CV_mult`), by induction on the list. Uses the classical Reals axioms (quarantined). Still per *finite* prime set; the infinite product / full `ζ` remains out of scope (`LEDGER.md`).

`EulerFactorR.v` is the **archimedean analytic layer**: it completes `PrimonGas`'s *finite* Euler product by taking the `K→∞` limit of one mode, proving the single **Euler factor as a genuine convergent series** over ℝ — `Σ_{k=0}^∞ p^{−ks} = 1/(1 − p⁻ˢ)` (`euler_factor`, for `p > 1`, `s > 0`). The bridge is exactly the intended one: the axiom-free ℚ identity `PrimonGas.geom_closed` (`(1−x)·psum = 1−xᴷ`) is the finite geometric closed form, which stdlib proves over ℝ as `tech3` (re-exposed here as `psumR_closed`); `geom_limit` then attaches the `K→∞` limit via stdlib's `GP_infinite` (with `xᴷ→0` for `|x|<1`), and `euler_factor` specialises to the fugacity `x = p⁻ˢ ∈ (0,1)`. **This file deliberately uses the classical Reals axioms** (`Print Assumptions` lists `classic`, `functional_extensionality_dep`, the `ClassicalDedekindReals` axioms) — it is the *quarantined* analytic layer, the same pattern as `LandauerBound.v` (real `ln 2`) vs `LandauerBoundL.v` (axiom-free). The ℚ core (`PrimonGas`, `LadderOps`, `FreeDivMeet`, `Padic`, …) remains "Closed under the global context"; nothing already built loses its purity. The full `ζ = ∏_all-primes` (infinite product) and `d/ds ζ` (term-by-term Dirichlet-series differentiation) are the deeper analytic targets — expressible over stdlib ℝ but a substantially larger build.

`LadderOps.v` builds the **ladder operators** and **differentiation over the lattice** — the second-quantised view of `PrimonGas`, over ℚ, axiom-free. On the occupation-number lattice of one mode the monomial `xᵏ` is the state of occupation `k`, and the calculus/ladder operators are **creation** `M : xᵏ ↦ xᵏ⁺¹`, **annihilation** `D : xᵏ ↦ k·xᵏ⁻¹` (the formal derivative `d/dx`), and **number** `N : xᵏ ↦ k·xᵏ`. The point: *differentiation over the (discrete) lattice IS the annihilation operator* `D = d/dx`, and the **number operator is `N = x·d/dx = M∘D`**. Proven: `MD` (`x·D(xᵏ) = N(xᵏ)`, the number operator termwise), `weyl` (the canonical/Weyl commutation relation `D(M xᵏ) − M(D xᵏ) = xᵏ`, i.e. `[D,M]=1`), and `deriv_number` (`x · d/dx` of the partition function `= Σₖ k·xᵏ`, the number-weighted / mean-occupation generating function `x d/dx Z`). The arithmetic ladder and the analytic ladder are the same operators: `create_mult` shows creation multiplies the underlying number by the base (`xᵏ⁺¹ = x·xᵏ`, so on `n = pᵏ` creation is "×p"), and `N` counts occupation = the p-adic valuation. `Print Assumptions ladder_ops` → **"Closed under the global context"** (pure ℚ, no Reals, no admits).

`PrimonGas.v` builds the **free Riemann gas** (primon gas) and its **Euler-product partition function**, over ℚ, axiom-free — the statistical-mechanics reading of the divisibility lattice. Each prime is a bosonic **mode**; an occupation vector `k = (kₚ)` is a state with Boltzmann weight `weight(k) = ∏ₚ xₚ^{kₚ}` (`xₚ` = fugacity of mode `p`, formally `p⁻ˢ`); the single-mode partition function is the truncated geometric series `psum x K = Σ_{k<K} xᵏ`. The **main theorem** `euler_product` proves the sum over *all* occupation states equals the *product* over modes of the single-mode partition functions: `Σ_states ∏ₚ xₚ^{kₚ} = ∏ₚ (Σ_{k<K} xₚᵏ)` — the finite Euler product `Z = ∏ₚ Zₚ`. The gas is **free** (non-interacting) precisely because the modes factorise, which *is* the independence of primes (coprimality / CRT) from `FreeDivMeet`; `euler_two_modes` states the two-prime form directly. With `xₚ = p⁻ˢ` and `K→∞` this is the Euler product for `ζ(s)`; the finite identity here is axiom-free. Supporting results: `qpow_add` (single-mode energy additivity → weight multiplicativity, mirror of `code_add`), `energy_additive` (the log/energy side `E(m·n) = E m + E n` is linear under state combination), and `geom_closed` (`(1−x)·Zₚ = 1−xᴷ`, so each `Zₚ` is the truncated Euler factor `(1−xᴷ)/(1−x) → 1/(1−x)`). `Print Assumptions primon_gas` → **"Closed under the global context"** (pure ℚ, no Reals, no admits). The `K→∞` limit (genuine `ζ` and the analytic Euler product) is the analysis-layer extension that would need ℝ.

`FreeDivMeetIso.v` fills `FreeDivMeet`'s **interior‑isomorphism** gap — the *full* CRT lattice iso, not just the axes. It proves `code(i,j) = pⁱ·qʲ` is an **order embedding** of the free product‑of‑chains lattice into the divisibility lattice: `code_order_iso : code u ∣ code v ↔ fle u v`. The forward direction (`code_mono`, order → divisibility) is a `Z.pow_add_r`/`ring` witness; the substantive reverse (`code_faithful`, divisibility → order) peels one prime at a time via **Gauss** + coprimality of prime powers (`FreeDivMeet.rp_pow_gen`/`pq_coprime`) and `pow_dvd_le` (`pⁱ ∣ pʲ ⇒ i≤j`). So the free lattice `F = ℕ×ℕ` sits inside `(ℤ,∣)` as *exactly* the `{p,q}`‑smooth numbers, and `Div(pᵃqᵇ) ≅ [0..a]×[0..b]` as posets. `Print Assumptions code_order_iso` → **"Closed under the global context"**.

`FreeDivMeet.v` introduces a **free unbounded lattice** and the **divisibility lattice**, and defines their **fixed point as the meet (coincidence) between the two** — a coincidence guaranteed, for any two prime powers, by **CRT**. The free unbounded lattice `F = ℕ×ℕ` is two *independent* unbounded chains (exponent axes for two places): meet = componentwise `min`, join = componentwise `max`, monoid sum = componentwise `+`, and it is a genuine lattice (`fmeet_glb`, `fjoin_lub`, absorption `fabsorb`) with **no top element** (`unbounded_above`). The divisibility lattice is `(ℤ, ∣)` with meet = `gcd`, join = `lcm`. The coding `code(a,b) = pᵃ·qᵇ` (two distinct primes `p,q`) embeds `F` into it as a monoid homomorphism (`code_add : code(u+v) = code u · code v`, `code_00 : code(0,0)=1`). The **fixed point** is where the two lattices' operations *coincide*, and on the two prime-power axis generators the coincidence is **exact**: `crt_lattice_coincidence` proves `gcd(pᵃ,qᵇ) = 1 = code(fmeet)` and `lcm(pᵃ,qᵇ) = pᵃ·qᵇ = code(fjoin)`. **CRT is the guarantee** — the whole argument runs through `pq_coprime` (distinct primes are coprime, via `prime_divisors`) and `rp_pow_gen` (coprimality lifts to prime powers, via `rel_prime_mult`); coprimality is exactly what forces the meet to be the free lattice's bottom `1` and the join to be the full product. `Print Assumptions free_div_meet` → **"Closed under the global context"** (Znumtheory / `Z`, no Reals, no admits). This is the honest lattice-theoretic core of CRT: the free product lattice sitting inside the divisibility lattice, meeting it exactly at the coprime prime-power axes.

`Padic.v` brings in genuine **non-archimedean topology machinery** — the **p-adic ultrametric** — over ℚ, axiom-free, as the *non-archimedean companion* of `BiView`. For a prime `p`, the p-adic absolute value of an element of valuation `v` is `|x|_p = p^{−v}`, which is **rational** (a power of `1/p`), so the whole norm lives over ℚ — no Reals, no completion, no topology library. Indexing by the valuation `v` (which *is* `BiView`'s global coordinate), we define `pabs v = 1 # p^v` and prove it is positive, `pabs 0 = 1` (units have norm 1), **multiplicative** (`pabs (v+w) = pabs v · pabs w` — valuation-additivity becomes norm-multiplicativity), **antitone** (larger valuation = smaller norm, "closer to 0"), and the **ultrametric / strong-triangle max-law** (`Nat.min v w ≤ u → pabs u ≤ max (pabs v) (pabs w)`: since `pabs` is a decreasing exponential of the valuation, a sum — whose valuation is `≥ min` — has norm `≤ max` of the two). **Wiring to `BiView`:** `arch_padic_opposite` shows `pabs` and `BiView.alpha` are the two views of one valuation moving in **opposite directions** — the archimedean view (`alpha`) *increases* while the p-adic view (`pabs`) *decreases* — the real and p-adic completions of a single arithmetic object. `Print Assumptions` **"Closed under the global context"** (no Reals, no topology). This is the genuine ultrametric *space* structure; the completion to ℚ_p (Cauchy limits) is deliberately out of scope.

`WalshHadamardHilbert.v` lifts the whole picture into a genuine (finite, 4-dimensional) real Hilbert space: it equips the signal space with the inner product ⟨f,g⟩ = Σ f g and proves it positive-definite, that the Hadamard operator is self-adjoint and satisfies Parseval (⟨Hf,Hg⟩ = 4⟨f,g⟩), that the normalized transform `Ur = ½H` is a real unitary involution (⟨Ur f, Ur g⟩ = ⟨f,g⟩, Ur² = I), that the apex reflection is a self-adjoint involution with orthogonal ±1 eigenspaces (nodes ⊥ antinodes), and that the standing-wave amplitude is a −1-eigenvector of norm²=2 preserved by `Ur`. Uses only the standard Coq `Reals` axioms; no custom axioms, no `admit`. (The infinite-dimensional ℓ²/L² lift the Hilbert–Pólya program needs is a much larger, analysis-library undertaking.)

**97 proof file(s):**

- `BiView.v`
- `BiViewProduct.v`
- `BitCountContrast.v`
- `BitDensity.v`
- `DiracDiagonal.v`
- `DiracOnCategory.v`
- `EigenSystem.v`
- `EulerFactorR.v`
- `EulerProductR.v`
- `FanoSelfAdjoint.v`
- `FiniteTopology.v`
- `ChainTower.v`
- `FredholmDirac.v`
- `FreeDivMeetIso.v`
- `PrimeFactorizationN.v`
- `PrimeFactorizationExists.v`
- `ProductFormulaQ.v`
- `ProfiniteCRT.v`
- `LocalGlobalCompat.v`
- `ComplexField.v`
- `RootsOfUnity.v`
- `DFTInversion.v`
- `DFTConvolution.v`
- `Parseval.v`
- `CharactersModN.v`
- `ZmodPStar.v`
- `Totient.v`
- `PolyRootsFp.v`
- `ZmodOrder.v`
- `PrimitiveRoot.v`
- `DirichletModP.v`
- `DirichletLEuler.v`
- `GaussianIntegers.v`
- `SumTwoSquares.v`
- `EulerReindex.v`
- `FreeDivMeet.v`
- `HeatFlow.v`
- `HeatFlowQ.v`
- `InvolutionQ.v`
- `Involution.v`
- `InvLimit.v`
- `KappaInvariant.v`
- `MassGap.v`
- `MassGapQ.v`
- `MobiusReciprocal.v`
- `INFMonoid.v`
- `INFProduct.v`
- `MobiusReciprocalR.v`
- `NxnZero.v`
- `KappaOmega.v`
- `KroneckerSelfAdjoint.v`
- `LadderDerivR.v`
- `LadderOps.v`
- `Landauer.v`
- `ObserverTriad.v`
- `ObserverSwap.v`
- `ObserverSpectral.v`
- `LandauerQ.v`
- `LandauerBound.v`
- `LandauerBoundL.v`
- `ProductFormula.v`
- `PosetMobiusFTC.v`
- `ProductFTC.v`
- `ProductMobius.v`
- `PosetTopology.v`
- `Padic.v`
- `PadicIntegers.v`
- `PadicMetric.v`
- `PadicRing.v`
- `PadicRingOpp.v`
- `PrimonGas.v`
- `PrimorialEuler.v`
- `PrimorialEulerBound.v`
- `PrimorialZeta.v`
- `PrimorialSpectralTheory.v`
- `SpectralAlgebra.v`
- `SpectralEvent.v`
- `SpectralPrism.v`
- `SpectralTripleRH.v`
- `SpectralTripleRH_closed.v`
- `StandingWave.v`
- `StandingWaveSpectrum.v`
- `TriadicSpectral.v`
- `VonMangoldt.v`
- `VonMangoldtR.v`
- `ZetaConverge.v`
- `RecipSquareBound.v`
- `EulerProductZetaBound.v`
- `EulerProductZeta.v`
- `AbelSummation.v`
- `VonMangoldtGlobal.v`
- `Chebyshev.v`
- `WalshHadamard.v`
- `WalshHadamardHilbert.v`
- `WalshSampling.v`
- `WalshUncertainty.v`
- `WalshHadamardHilbertQ.v`
